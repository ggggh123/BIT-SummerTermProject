# AI 工作数据（未允许勿动工作区）
.workbuddy/

# CodeBuddy 本地工作数据（计划/会话状态，仅本地保留，不进仓库）
.codebuddy/

# 本机协作会话与个人 CMake 预设
.superpowers/
CMakeUserPresets.json

# 本地临时脚本（一次性工具，永不提交）
.local-tools/

# Web dashboard local artifacts (generated, never commit)
dashboard/.vendor-cache/
dashboard/runtime/

# 第二阶段：数据交接包与大体积产物（默认不入库，走共享文件夹/网盘；封存批次的演示包见文件末尾例外）
# 用 handoff/* 而非 handoff/ —— 目录整体排除时无法再放行子文件
handoff/*
*.parquet
spark-*-bin-hadoop3.tgz
_artifact_env.trash-*/

# Web 工程依赖与构建产物（dist 由 Flask 托管，构建生成不入库）
node_modules/
web/dist/

/build/
/.venv/
/runtime/
**/runtime/
*.user
*.user.*
*.db
*.db-wal
*.db-shm
*.log
# 已审阅的 Qt 回归日志作为文档证据入库，活动运行日志仍排除。
!docs/design/client-feedback-2026-09-08/ctest-*.log
!docs/design/ui-feedback-followup-2026-09-08/*.log
!docs/test/evidence/final-2026-09-08/*.log
config.local.ini
.env
web/test-results/
web/playwright-report/
# 演示服务的运行日志与 PID（part2/scripts/start_part2.sh 写入；pid 不能被提交）
/logs/

# Python
__pycache__/
*.pyc
*.pyo

# Part2 generated data handoff packages
/part2/scml/handoff/ods/
/part2/scml/handoff/dwd/
/part2/scml/handoff/dws/
/part2/scml/handoff/ads/

# 例外：2026-09-15 封存批次的演示交接包随仓库分发（约 5.7MB，克隆仓库即可复现大屏）
# —— ADS 只读库（与 ads_manifest 哈希一致）+ ML 预测交付包 + 清洗质量报告。
# 注意：这些规则必须排在 `*.db` / `*.log` 之后才生效；其余 handoff 产物仍按上面的策略排除，
# 历史快照（ads.db.pre-policy-freeze、sealed-original-20260915235603/）依旧不入库，走共享文件夹。
handoff/*
!handoff/ads/
!handoff/forecast/
!handoff/prl-20260915T133614Z-56524/
handoff/ads/*
!handoff/ads/ads.db
!handoff/ads/ads_manifest.json
!handoff/ads/quality-publish-receipt.json
!handoff/forecast/*
!handoff/prl-20260915T133614Z-56524/*
