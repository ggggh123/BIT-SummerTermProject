fix(part2-02): Q9 改为仅置空坐标（不再整行隔离）+ 更正三处错误归因

一、Q9 越界坐标处置（契约 §7 待办 4，#2 决定）
   由「整行隔离（reject）」改为「仅置空该字段并保留站点记录（repair）」。
   理由：stations 是维度表，被订单/充电桩/遥测引用；整行剔除会使该站关联事实全部变成
   孤儿引用并触发 Q7 级联——实测使 DWD 站点由 8 降至 7、订单由 12 万降至 102,332。
   置空字段既不臆造坐标，也不牵连其他数据。「回填坐标」因无外部权威数据源已排除。

   改动：part2/quality/record_rules.py 的 Q9 分支 -> 置空字段 + repair；
         contracts/quality-policy-v0.1.json 的 bbox_note 同步；
         plans/03 §3.2 处置行明确；
         tests/test_record_rules.py::test_coordinate_bounds 更新为新行为
         （含「坐标确实被置空」与「不再进 reject」两条断言）。
   安全性：reject -> repair 不会触发整行剔除（剔除判据见 quality/rules.py:39，
   仅含 reject/deduplicate）。

二、更正三处把「平均等待恒为 0」解释错的文档（#1 在 dev 的 defect-log §D14 指出）
   - part2/scml/warehouse/README.md 第 6 条
   - server/README.md 的 avgWaitNote 行
   - part2/scml/warehouse/jobs/_ads_extras.py 写入 ads_meta.avgWaitNote 的文案
   正确解释：DWD 时间列是 STRING，Spark 3.5.7 上直接 UNIX_TIMESTAMP(字符串) 返回 null，
   相减与 AVG 静默变 0——是 bug，不是「ODS 未建模」。SQL 已于 6524503 修好，
   但修复前物化的批次（含当前演示批次）该值仍为 0，重跑后恢复。

三、测试可移植性：4 个测试/脚本里的 read_text() 补上 encoding="utf-8"
   （Windows 默认 GBK 会读不了含中文的 JSON，Linux 不受影响）

验证：集成机（Linux/UTF-8）全量单测 81 项全绿。