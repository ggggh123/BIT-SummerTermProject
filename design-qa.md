# Qt 首批竖屏 UI 视觉核验

> 2026-09-06 后续：充电/结算 UI 已按既定方向补齐本批视觉检查，详见[独立记录与 720/844 截图](docs/test/user-charge-ui-2026-09-06.md)。本文件下方保留首批历史；后续记录也不代表历史/账户/导航外观已全部完成。

状态：首批标准页面及缺站异常状态视觉核验通过，整批代码/回归结论见验证记录。final result: passed

## 比较目标与证据

- 设计：工作区 `design-drafts/2026-09-05-user-client-v2-mobile/01-login.png`（853×1844），`02-nearby.png`（853×1844），V3 `04-station-detail.png`（852×1846）。完整源路径前缀为 `/mnt/hgfs/Desktop/SummerTermProject/`。
- 实现：`docs/test/evidence/user-mobile-ui-2026-09-05/login-390x844.png`（853×1846）等真实 QWidget::grab 截图，非生成图。
- 逻辑视口：390×844 与390×720；`QT_SCALE_FACTOR=2.18718`，截图宽853像素，和源稿近似同密度。2像素高度差是概念稿比例/像素取整差异，不作为缺陷。
- 首轮源稿与 Qt 截图在同一图像比较输入中打开。源稿为空手机号/已连接，首轮实际截图为已输入手机号/未连接；状态差异不视为业务错误，后续补充匹配状态与独立离线截图。
- 本项目为 Linux 原生 Qt，不适用浏览器、CSS视口、手机框架运行时或网站部署检查。

## 迭代1：登录

- [P2] 插画呈现为内嵌矩形照片：当前20px内缩画布和外层浅绿底色形成清晰矩形边界；源稿为连续的欢迎插画区域。已反馈给实现者：图片铺满欢迎区域宽度，标题保留20px边距，协调背景边缘，保持等比与小屏表单可达，不修改原始位图。
- [P3] 顶部产品名当前使用深色；可用主题青绿恢复源稿的品牌文字层级，不添加虚构品牌图标。

## 五项必查面

- 字体：实际中文字体清晰，标题/正文/辅助信息分层明确；源稿与Qt并非逐像素字形复制。仍需最终小屏截图核验。
- 间距：首轮主体顺序符合欢迎→表单→连接说明，表单可见；插画区域的矩形边界需修正。底部留白中不保留概念稿的“UI设计稿”水印。
- 颜色：主要操作为青绿、白色表单、浅色背景；真实离线提示为红色，不伪装成已连接。
- 图片：使用独立生成1615×974插画，没有整页栅格化或手绘替代；车辆/充电桩完整，无文字控件嵌入。需改善背景整合。
- 文案：登录/注册、自动创建账户、11位手机号符合现有功能，无短信或第三方登录；截图数据状态需和目标注明差异。

## 迭代2：登录复核

- 实现者将标题边距与图像布局分开，扩大图片可用宽度、协调欢迎区背景为#F0F9F7，产品名恢复青绿，没有修改原始位图。
- 最新 `login-390x844.png` 与源稿以及 `login-390x720.png` 在同一比较输入打开。截图为空手机号、已连接的受控UI场景；另有 `login-offline-390x720.png` 保留真实离线说明的显示测试。
- 字体、间距、颜色、图片和文案重新检查：两种高度的表单/主要按钮/连接信息均可见，插画完整等比，焦点描边清晰，首轮P2已解决。
- [P3] 插画背景的轻微边缘色差仍可进一步精修；原稿文字与汽车部分重叠，Qt按明确上下区域排布，是为缩放与表单可达性作出的可接受差异。概念稿底部水印和无授权品牌图案未加入产品。
- 登录页面可进入代码审查；整体仍 blocked，等待找站/详情实施与验证，不提前宣布全批完成。

## 首次底部导航检查

- `authenticated-shell-390x720.png` 确认底部栏在720px视口可见，没有被中部内容挤出。
- [P2] 首轮导航为图标左/短标签右；源稿为图标上/完整标签下。已反馈恢复竖向图文布局和找桩/历史订单/我的账户（条件显示当前订单），保留QPushButton信号与已有导航限制。
- 后续截图与V2找站源稿在同一比较输入复核：底部导航已经改为图标上/完整标签下，选中项有浅绿背景，720px窗口底部完整可见；该P2已解决。中部仍为未改造找站页，仅作为壳的验证，不作为找站视觉验收。

## 迭代1：找站与详情

- V2找站、V3详情源图与实际390×844截图在同一图像输入对照；实际初稿已保留在 `docs/test/evidence/user-mobile-ui-2026-09-05/qa-iteration1/nearby-390x844.png` 和 `detail-390x844.png`。
- [P2] 找站首卡上方占用偏高、卡片偏高：设计稿可见3站，Qt初稿约2.2站。要求压紧冗余布局间距，同时保留48px主操作、16px卡片内边距和13–14px辅助文字。
- [P2] 价格单位“/ 度”与数值一样大，削弱数字层级并撑高卡片；分离数字与小号单位。
- [P2] 地点可编辑下拉框右侧出现原生黑色边框突起；统一下拉/editor边框与圆角视觉。
- [P2] 详情普通充电桩元数据分成过多行，仅约2.5桩可见；压紧普通ID/编号/类型/额定功率排布，长编号仍允许自然换行，不删真实字段。
- 状态差异：详情源稿4桩、实际fixture5桩不属于错误，后续用4桩标准场景对照，同时保留5桩与长内容测试。源稿零空闲站操作灰色不作为整体禁用依据，实际仍应允许查看站点和导航。
- 本轮P2修复与复核未完成，整体继续 blocked。

## 迭代2–3：找站与详情复核

- 同密度源稿与修订后的 `nearby-390x844.png`、`detail-390x844.png` 再次放入同一图像输入，实际检查价格/单位、下拉框右缘、站卡操作和充电桩各行。853像素宽原图中的这些区域足够清晰可读，未另做缩略局部裁剪。
- 详情四桩标准场景已恢复完整四行首屏呈现；ID与编号同排、类型和额定功率在次级行，状态与选择在右侧。长编号自然增高，可滚动到第16桩及其选择按钮。首轮密度P2已解决。
- 价格数字/单位分层，地点下拉使用同库现成箭头，黑框瑕疵消失。
- 列表第二轮第三卡操作贴近底栏，进一步压紧内部空隙后第三轮的三站价格、计数和查看入口均可见；48px触控尺寸与16px内边距保留。卡片最下缘接近滚动视口，不影响主要操作。
- [P2，正在核查] 第三轮844截图第一张站卡的“/ 度”缺失，而720截图及其他卡正常；已反馈实现者核实，未据其他测试通过忽略此问题。
- 已另打开390×720列表/详情、长详情末项、零空闲及离线证据。标准720场景通过纵向滚动容纳其余内容，固定导航仍可见；零空闲及离线截图为裸NearbyPage受控测试，不含MainWindow导航栏，不用于证明完整壳布局。
- 可接受差异：实际使用批准的较大按钮和完整额定功率/编号，详情预约提示比概念稿更靠下、可滚动到达；无有效预测时不保留“暂无预测”装饰。示例第三站价格及第二站空闲数不同属于fixture值差异，不写入生产数据。

## 后续核验清单

- 修复后重新捕获登录全图与局部可读控件，保留上轮问题/修复/新证据。
- 对照找站与详情，覆盖长名称、不可选状态、空闲数为零和离线返回。
- 对实际窗口底部导航、字体、图标和滚动范围进行最终检查。
- 完成后将 final result 改为 passed 或保留 blocked 并说明未解决问题，不能以自动化测试替代视觉检查。

## 迭代4与首批视觉结论

- 价格漏绘通过拆分为独立原生数字/单位标签解决；两个高度新增完整行高和视口矩形断言，先RED后GREEN。最新844截图与V2源稿在同一图像输入再次比较，三站价格单位均完整，第三站查看入口没有被底栏遮盖。本问题的中间帧保留于本次工具对照历史，最终PNG已更新。
- 最终实现证据复制到 `docs/test/evidence/user-mobile-ui-2026-09-05/`；初轮找站/详情证据继续保留在 `qa-iteration1/`。标准截图为真实MainWindow与受控TCP测试响应，裸页专项截图已在上一节明确区分。
- 字体：Noto Sans CJK SC，标题与数值显著、单位为正常字重小号、正文和元数据清楚。标准与长内容均能阅读；较概念稿略大的可操作控件遵循批准尺寸，不把长编号强制截断。
- 间距：单列构图、20px外围边距、16px卡片内距、固定底栏；通过收紧内部冗余空隙，844标准列表可见三站主要信息/操作，详情可见四桩。720及长内容依赖真实纵向滚动，不挤出底栏。卡片贴近视口边缘是滚动裁切而非覆盖操作。
- 颜色：青绿主操作、浅灰背景、白卡和统一边框；故障为危险红，禁用仅作用于选择按钮。离线状态有明确缓存文案，不把连接失败画成成功。
- 图像/图标：登录使用独立插画，非整页栅格背景；图标来自同一现成Material库，未用手绘SVG或字符拼图替代。图片主体等比清晰，保留轻微底色边界P3。
- 文案：使用现有站点/桩/价格模型；“额定功率”不冒充实时功率；返回不自动预约、零空闲仍能查看/导航；没有SOC、虚构支付方式、原稿水印或“示例数据”写入生产界面。
- 剩余P3：登录插画轻微底色边界；未选中导航图标仍为青绿、文本灰色，选中背景与字重仍可区分。交最终代码审查一并判定，不作为已解决事项隐藏。
- 无未解决P0/P1/P2视觉问题。本结论仅覆盖已批准首批页面，不宣称后续导航/订单/结算完整重设计已交付，也不替代真实服务端联调。

## 代码审查补充：刷新缺站空态

- [P2] 已加载详情后，权威刷新不再包含该站点，清空详情却没有恢复此前隐藏的提示标签。用户可能只看到空白详情内容；已派回实现者修复并新增真实刷新可见性回归与空态截图。
- 标准页面上述视觉结论保留；此新状态复核前整体重新标为 blocked。

修复后，controller实际打开 `docs/test/evidence/user-mobile-ui-2026-09-05/detail-missing-station-list-first-390x844.png`：853×1846完整主窗口中，原因文案、返回箭头、页头与底栏均清晰可见，没有残留旧站旧桩或假成功信息。另一个回包顺序亦有留图。此异常状态没有独立源概念稿，按已有反馈可见性要求核验，不声称像素级复刻；P2已解决。

2026-09-06归档复验确认两种回包顺序的图片SHA-256相同，表示它们收敛到相同最终空态，并非遗漏视觉变体。复验最初受一次多图工具输出影响而误判为不含完整壳；随后以原始分辨率单独打开同一仓库文件，确认返回箭头、“站点详情”页头和底部三项导航均存在，故保留上述完整主窗口结论。

Controller最终独立重捕mobileui全部截图：16/16通过、3014ms。再将最终登录图与V2源稿同次打开复核，共享样式没有破坏表单/插画/连接说明；标准详情价格区域亦在最终修复后与V3同次对照。上述P3差异维持原分类。

## 终审一次精修波次

- 终审Minor：未选中导航图标仍青绿，详情离线提示为灰。已修复为按真实文字palette着色的原图标（保留官方路径）及动态danger/secondary状态角色。
- 高DPI绘制初次出现图标局部裁切，补真实着色包围盒RED后修正设备无关绘制坐标，复测通过；该中间缺陷不作为最终可接受差异保留。
- Controller把V2源稿与最终 `nearby-390x844.png` 放入同一输入复核：选中图标青绿、未选中图标灰色，与标签一致；三站主要信息/单位/操作仍完整可见，图标路径没有裁切。
- Controller另实际打开 `detail-offline-390x844.png` 与 `detail-recovered-390x844.png`：离线提示为危险红，恢复后为次级灰并注明信息仍为缓存，没有伪装成已刷新。保留不可用桩禁用、元数据可读。
- 最终只剩登录插画轻微底色边界P3，按已记录取舍延期。上述图已复制到正式证据目录；该波次聚焦代码复核确认两项修复到位且无新Critical/Important，不再增加视觉改造范围。

final result: passed
