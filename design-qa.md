# 能量脉冲 · 电桩状态与系统健康视觉验收

日期：2026-09-08。本轮范围是用户批准的两个管理页结构级改造。已通过的双端主画面与模拟器保持原有成果；上一轮报告保存在 `docs/design/operations-qt-2026-09-08/previous-simulator-design-qa.md`。

## 视觉依据、实现与比较状态

- 视觉依据：`docs/design/energy-pulse-qt-2026-09-08/captures/pulse-admin-1360x860.png`，用户已通过的原生运营总览；最初选定方案仍在同目录 `reference-frame.html?concept=pulse&view=admin`。
- 本轮实现：`docs/design/operations-qt-2026-09-08/captures/fleet-selected-1360x860.png` 和 `health-selected-1360x860.png`。以下新截图名均相对该 `captures/` 目录。
- 新管理页面没有独立原始设计稿，本次是已通过视觉系统的业务延展，不宣称不同信息结构逐像素相同。
- 参考与实现均为 **1360×860、1× PNG / Qt 逻辑像素**，无桌面窗口外框。小窗口补查 **1280×720**。
- 参考为双设备设计测试样本，新页为隔离黄金库副本，6 个站点、48 台设备及已选中故障设备；比较主题、排版与操作结构，不比较数值是否相等。

## 同一输入的联合对照证据

联合页面：`docs/design/operations-qt-2026-09-08/compare.html`，分别使用 `?page=fleet` 和 `?page=health`。已在本机浏览器打开并抓取完整页面，同一截图同时包含参考与实现全景以及两块局部，非分开看图后凭记忆判断。

- 全景两张原生截图等比显示为各 **414×约262**；没有拉伸、图片重绘或改变原应用内容。
- 局部均为 **268×260、1:1**，从原生 PNG 在查看页裁切。参考起点 `(1080,183)`；电桩状态 `(1070,291)`；健康页 `(1055,178)`。比较主数字、辅助文字、状态颜色、边距与边框。
- 联合证据浏览器完整截图约 1265×754；页面内容固定 880 px 宽用于证据。面向用户的 `index.html` 响应式排列，窄窗口改为上下排，避免右側对照被截断。
- 另直接检查默认、列表、重启中、空状态、读失败、健康明细与小窗口截图，不能仅凭缩小全景判定密集文字可读。

## 发现与迭代

1. **P2，健康页旧维护区白底与浅色文字不匹配。**
   原状态见 `docs/design/energy-pulse-qt-2026-09-08/captures/health.png`。重做时改为深色暖橙边框区域，与普通诊断区分开；按钮和清除范围保持可读。修后：`health-selected-1360x860.png`、`health-compact.png`。

2. **P2，首轮设备格在较矮窗口重叠。**
   证据：`fleet-compact-first-pass.png`。Qt 样式表按钮高度与布局最小行高未一致约束，布局压缩时内容重叠。
   修复：设备网格明确 60 px 最小行高，标题行最小 26 px，站点及外层网格设置最小尺寸约束，由滚动区域承接溢出。新增矩形不重叠、文本与按钮高度断言。
   修后：`fleet-compact-1280x720.png`，设备两行分隔清晰，不相互覆盖。

3. **P2，从健康页定位设备后首帧阵列未及时显示。**
   证据：`fleet-selected-first-pass.png`，右侧已有设备但左侧为空。
   修复：筛选未变化时复用已有控件，只更新选择；下一轮事件中完成布局与滚动定位，避免使用页签切换前的几何信息。新增选中设备实际可见且位于阵列视口内的断言。
   修后：`fleet-selected-1360x860.png`，1010 故障桩、站点分组与右侧详情同时可见。

4. **信息层次改进。**
   第一版健康中心说明文字偏多，见 `health-first-pass.png`。改为真实五状态计数及占比分条，原始诊断明细放在可切换视图中；保留简洁数据口径说明，没有用装饰性假指标填充页面。

5. **联合查看页窄窗口修正与最终比较。**
   窄侧栏下初版并排局部超出可视宽度，改为响应式上下排列；另使用只读联合证据页完整捕获同一输入的全景和局部。最终比较无剩余可操作的 P0/P1/P2 视觉问题。

## 五个必检表面

| 表面 | 检查与结论 |
|---|---|
| 字体与排版 | 继续使用 Noto Sans CJK SC；总量／故障数 49 px、设备编号 34 px、额定功率 27 px、状态数字 29 px、正文 13 px、辅助 11–12 px。主次层级延续原主题；设备小格代码与状态分行，长编号有省略及完整 tooltip。故障计数比普通详情大是有意强调，不照抄参考功率值字号。 |
| 间距与布局 | 左侧 164 px 导航、主区约 29 px 边距、12–24 px 间距保持一致；设备面板 252 px、健康队列 268 px，依据字段长度调整。1360×860 主区可用；48 台设备阵列本身滚动，1280×720 内容滚动，不压扁设备格。维护操作在健康页滚动区之外。 |
| 颜色与状态 | 深蓝 `#102536`、面板 `#193444`、正文 `#eaf3f6`、辅助字 `#97adbc`、青绿 `#72ddc3`；故障暖橙、预约／重启中暖黄。状态同时有文字，不只靠颜色。浅青绿按钮使用深色字，读失败显著提示并停用设备操作。 |
| 图形与资产 | 状态分条来自实际计数，不是装饰图或虚构曲线；设备阵列为可交互按钮。复用已有控件与图标，不新增插画、位图、网络字体或图表依赖；未用代码形状替代原设计中的照片、标志或插画。 |
| 文案与内容 | 额定功率与采样功率区分；最后遥测记录不是在线心跳。监听／数据库读取／遥测分别说明，不制造综合健康分。空数据和读取失败分开，故障定位与黄金库复位用途分开，清除范围与确认中文可读。 |

## 操作与回归

- 全量 CMake/Ninja 构建通过；CTest **36/36 测试组通过，39.16 秒**，包含真实三程序隔离联调、后台刷新、权限及演示复位回归。
- 管理端专项 **16/16 项通过**，包含初始化、清理和按需截图。本次增加四个测试函数覆盖只读视图、筛选定位、实际重启和异常／空状态。
- 黄金库副本中故障设备 10：默认取消不写重启审计；确认后只有一条重启请求；先进入 `restarting`，再由真实服务端转换到 `idle`，不是测试直接改标签。
- 按站点 ID 分组、重名站点不合并；筛选后清除无效选择；按 ID 定位；读错误禁用操作；单次在途控制及 ACK 后等待新快照均有断言。
- 1280×720 检查按钮可达、无横向溢出、设备格不重叠。电桩页小窗口截图为滚到操作区的状态，顶部总数离开视口属于正常滚动；健康页维护入口持续可见。
- 原生抓图来自 Qt offscreen，未操作正式数据库。空状态／读失败／监听停止通过隔离 UI 输入覆盖，不等同于真实拔网线或损坏数据库测试。
- 留档状态选择器、页面链接与图像加载经过浏览器检查；HTML 不能执行 Qt 控制。

## 残余边界与交付清单

- [x] 两页结构级实现，保持业务查询、确认与复位约束。
- [x] 全景及 1:1 局部联合对照、发现问题后的同尺寸重抓。
- [x] 正常、故障、重启中、空数据、读失败、小窗口回归。
- [x] 新旧文档入口衔接，明确其余管理页不是本轮逐页重做。
- [ ] 用户／队友现场鼠标联演及最终审美确认。
- [ ] 真正 Ubuntu 22.04 / Qt 6.2、不同字体及 DPI 换机验收。本机为 Ubuntu 25.04 / Qt 6.8.3，未下载系统镜像；不能将本机通过写成换机通过。
- [ ] 提交、推送、合并、主启动目录及发行包同步（本轮未操作）。

P3：诊断表在较矮窗口需要纵向滚动；长编号／地址通过 tooltip 阅读全值。不同 Qt／桌面字体栅格化和缩放尚待队友机器验证，不对所有 DPI 作已验证承诺。

final result: passed
