# 核心联调缺陷交接单（2026-09-06）

> 状态更新：下方保留修前缺陷及原模块归属；原始三个 P0、模拟器/数据工具、管理端运行项已由本机补交并完成独立审查。**当前本地修复、同版本测试与尚未关闭项以[修复后复验记录](../test/core-fixes-2026-09-06.md)为准**，不是已通知队友、已合入共享分支或已通过最终验收的证明。

> 后续授权更新：2026-09-06 用户已允许本机直接介入服务端、数据库和模拟器修复。以下分工保留为模块归属与审查交接依据，不再是本机只能等待队友的限制；修复前现象仍按原基线记录。

## 共同基线

- 服务端源码来源：`origin/feat/server@661c91e3dbe252628b4b8411a73495d9d3137005`。
- 模拟器与核心黄金库来源：`origin/feat/data@10034fd3be016a5cb53f1ae417451b590ae6e50a`。
- 共享协议与用户业务基线：`origin/dev@e21c73a9f3d118cac169cfeb7feb3d6d7f7d6f02`。
- 本机候选分支：`integration/core-20260906`。来源目录按快照组合，**不是功能分支已正式 merge 到 dev/main**；修前测试见 `docs/test/core-integration-baseline-2026-09-06.md`，修后版本与测试见 `docs/test/core-fixes-2026-09-06.md`。

修前证据和原因定位见 `docs/test/server-progress-2026-09-06.md`。该历史探针记录的是原始版本，不应用于判断修后 HEAD 仍有以下三个 P0。

## 当前回交状态

- #2 对应补丁：时间/计费/事务/持久化幂等 `cfa00e7+dcd7d5a`；管理运行/刷新 `9fc9919+237e2b0`。均已独立审查通过并组合到本地候选。
- #4 对应补丁：模拟器线上时间、权威同步/重发、生产时钟、core 清单及受控新副本 `6e44c18+6a69d0b`。均已独立审查通过并组合。
- #3 保持生产 UserApi 严格校验，真实 Qt 四组流程通过；已归档手机式 UI 保留，未因服务端修复改宽客户端合同。
- 组合生产基线 `2318d14`：全新构建成功，CTest 24/24，数据库 15/15；实际模拟器跨模块测试继续补充，不称为人工三端/腾讯地图彩排。
- 下一个服务端工程缺口是专属串行 DatabaseWorker 和合同 `demo.reset`；完整异常路径、第二批 UI、同 SHA 双彩排与共享分支发布也仍需完成。

以下各节是**原始缺陷交接要求**，保留用于核对补丁覆盖，不表示仍在等待队友从零处理。

## 给 #2 TL：优先修复三个 P0

| 编号 | 复现与现象 | 重点代码 | 修复交接应提供的证据 |
|---|---|---|---|
| P0-1 时间戳 | 黄金库老用户登录成功，但预约被 Qt 拒绝；新用户登录被拒绝；回包缺少 `+08:00` | `AuthService.cpp`、`UserService.cpp`、`DatabaseManager.cpp`；对照客户端 `ContractTimestamp.cpp` | 新老用户的真实 Qt 登录、预约通过；用户、桩、订单、设备 ACK 时间均合法，UTC/上海进程环境均通过 |
| P0-2 量价与时长 | 0.25+0.50 kWh 遥测累计 0.75 kWh/113 分；停止后被改成 1 kWh/100 分/3600 秒 | `UserService::stop/settle/orderObject`、`TelemetryService::orderObject` | 停止前后量价保持，时长取实际时间，结算余额差等于订单金额，管理统计一致 |
| P0-3 重复执行 | 同 requestId 两次充值 100 分，余额 50000→50100→50200；重复遥测返回冲突而非原 ACK | `ApiServer`、`RequestLogService` 和对应业务事务 | 充值/建单/遥测/结算只执行一次，重复请求返回首次 ACK；SQL 失败无半笔业务 |

注意：不要通过放宽客户端 Timestamp 校验或删除重试测试来消除红灯。`charge.stop` 后订单仍处于 charging 并带 endedAt、等待 settle，是现有合同设计，不要新增 stopped 状态。

第二梯队：管理 GUI 使用指定数据库的启动入口（目前 `--db` 会触发无界面模式）、月营收计算、管理页面刷新，以及故障恢复后的未结算占用 guard。详细现象在检查报告中，先保证三个 P0。

## 给 #4 SCML：模拟器与黄金库

1. **线上时间精度**：当前 `TelemetryEngine` 的毫秒递增在 `SimulatorClient::isoPlus08` 编码时丢失。测试应检查实际 JSON 中 `.001/.002` 仍有先后，不只比较内存 QDateTime。
2. **权威桩状态同步**：客户端开始/停止、管理端重启后，模拟器需要及时获取最新状态；首轮若依赖人工刷新，必须明确写进操作步骤。
3. **黄金库使用方式**：先校验 `core.db.sha256`，然后运行副本；服务端启动会迁移 request_log，不能把封存原件直接启动为运行库。
4. **复位**：当前没有自动复位实现。可先准备退出三端后生成新运行副本的冷启动流程，不在运行时覆盖 SQLite。`demo.reset` 已是冻结合同第 27 条的管理员 action，目前属于实现缺口；冷启动流程不关闭该缺口。
5. **封存脚本**：重建先输出到新目录；现有 `build_golden.py` 固定写 `manifest.json`，不得覆盖旧 demo 的清单。

## 给 #3 / #5：复测入口

本机现可直接补交相关生产代码修复；修复必须保留来源、对应缺陷及真实回归结果，不以修改客户端合同校验代替服务端修复。

- 完整构建与 CTest：按基线记录执行，加入新模拟器/UI 后不能复用旧 19/19 记录。
- 地图 HTML：`node --test apps/user-client/tests/test_navigation_html.mjs`，离线执行，不消耗腾讯在线额度。
- 数据库：`python3 -m pytest database/tests -q`。
- 真实服务端探针：`docs/test/evidence/core-integration-2026-09-06/README.md` 有独立运行方法；退出码 0 只表示诊断完成，必须对照打印的实际业务值。
- 完整 Qt 与三端联动：需后续真实回包、管理窗口、模拟器和黄金库运行副本的共同验证，探针不能代替。

## 回交格式

队友完成修复后，请提供：功能分支和提交 SHA、修改文件、对应缺陷编号、修前/修后命令与结果、运行库来源和是否需要迁移。先由本机在独立集成候选复验，再走既定 `feat/* -> dev -> main` 流程。

修复期间继续以核心三端为主线；Web/ML 成果保留但不加入发布门槛。需求矩阵由队员人工维护，本交接单不操作它。
