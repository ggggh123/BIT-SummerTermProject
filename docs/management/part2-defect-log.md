# 第二阶段缺陷记录

> 维护人：#1 王浩恩（PM）｜日期：2026-09-15
> 只记**已经核实过**的缺陷：每条都写清现象、影响、归属和当前状态，便于答辩时如实交代。
> 「已修」的都指向具体提交或 PR；「开放」的说明卡在谁那里。

## 1. 已修

| # | 缺陷 | 现象与影响 | 归属 | 修复 |
|---|---|---|---|---|
| D1 | 主页「近 30 日营收趋势」实际只有 7 个点 | 接口清单里主页的 `revenue-trend` 写死 `days=7`，视图层再 `slice(-30)` 也只能拿到 7 个点；图上 7 根柱子挂着「近 30 日」的标题，答辩一眼可见 | #1 | `web/src/api/endpoints.js` 改 `days=30`；新增单测（对准页面真正调用的 `buildRevenueTrendOption30d`）与第 8 条 E2E 断言；PR #18 |
| D2 | E2E 断言写死了本地批的数字 | 断言里写死「112,422 单」「3 号站」，对着官方 PRL 批（97,804 单、站点 id 1/2/4/5/6/7/8，没有 3 号站）必挂 | #1 | 改成从接口返回的真实站点列表取站点、比较相对变化；PR #18 |
| D3 | `web/.env.production` 缺失导致 `npm run build` 出的是 mock 构建 | 该文件在 dev 上（提交 `5a32e55`），但 `feat/part2-web-layout` 落后 dev 20 个提交时没有它；照文档跑 `npm run build` 会构建出 mock 版 dist，演示数据全错 | #1 | 把 `origin/dev` 合并进分支（`8b7c218`），构建产物经检查已不含 mock 数据 |
| D4 | 文档口径停留在旧批 | `server/README.md` 写「287 桩」、`docs/design/part2-api-contract.md` 写「8 站 × 24 点」、`part2-metric-checklist.md` 整表是本地 Python 物化批——与官方 PRL 批（251 桩 / 7 站）不是一套数据 | #1 | 三处均已按官方口径改写，并说明两者差异来源 |
| D5 | 主页 KPI 容易被读成窗口口径「全城峰值负荷」 | `/api/gov/peak-load` 只返回**最近一天** 24 点，大屏 KPI 因此是当日峰值 7,153 kW；窗口内真正最大是 7,964.665 kW（2026-07-10 18 时），差约 10% | #1（前端标注） | KPI 标题改为「全城峰值负荷（当日）」，提示行补上具体日期与「非窗口峰值」；单测与 E2E 都钉住这个口径 |
| D6 | 企业页「新增用户」看起来像数据坏了 | `ads_daily.new_user_cnt` 整列为 0，图上是一条贴地零线。真实口径是**窗口内完成首单的新客**，而生成器的 4,985 个用户注册时间全部早于业务窗口 | #1（前端标注） | 图例与系列名改为「窗口内首单新客」，图下加口径说明；构造器挪进 `lib/charts/enterprise.js` 便于测试 |
| D13 | E2E「点击地图站点」偶发失败（约 10 次里挂 1 次） | 该用例在地图底图（GeoJSON）到位前就去取 ECharts 实例。`mapReady=false` 时图表还是经纬度散点（没有 `geo`），`find(...geo)` 返回 `undefined` → evaluate 里**硬抛错**，Playwright 不会重试，于是直接失败 | #1 | 加显式等待：`waitForFunction` 等到出现带 `geo` 的图表再取实例；并加复现开关 `PART2_SLOW_MAP=<ms>`。**反证过**：把底图延迟 1.5s，改前必失败（报 `找不到地图图表实例`），改后 9/9 通过 |

## 2. 开放（需别人闭环）

| # | 缺陷 | 现象与影响 | 归属 | 现状与建议 |
|---|---|---|---|---|
| D7 | `run_dws_ads.sh` 内置对账「假报警」 | 本机实测 24/30、6 项失败：① 小时表容差写死 0（15,073 vs 15,120）② 「重算」用 SCML 的 Python 口径（112,422 单）与 PRL 口径（97,804 单）不可比 ③ 找 DWD 时不认 `dt=` 分区目录。**不是数据问题**，但答辩当场跑对账会尴尬 | #3 / #4 | 临时规避：`53-run-dws-ads.sh --skip-reconcile`；建议按 PRL 口径改基准并支持分区目录 |
| D8 | ML 批次被一次 ADS 重建静默丢掉 | 2026-09-15 15:00 在本机重建 `ads.db` 时没带 `FORECAST_HANDOFF`，预测三表退回 seasonal-naive 基线（`is_baseline=1`），而 dev 文档记的是真实 MLlib 批次 `ml-20260915-111604` | #4 / #5 | **规则不变**：重建 ADS 一律带上 `--forecast-handoff`（或构建时设 `FORECAST_HANDOFF=handoff/forecast`）。**现状（09-15 晚，按 §2.1 决策）**：改为在演示机重跑整链路，DWS/ADS 构建时**一次成型**写入真实 MLlib 批次，**不再等待 #5 的 `forecast.db` 交接**；#5 那份批次保留为「方法可在目标机复现」的证据 |
| D9 | 预测站点集合与 ADS 不一致会被拒合并 | `merge_ads.py` 硬校验「预测站点集合 == `ads_station.forecast_enabled=1` 的集合」，并校验每站 `busy + idle == charger_cnt`。官方 ADS 是 7 站 / 5 个启用站（120 点）；早前那批 8 站（192 点）来自自测的 17,280 行合成数据，会被直接拒绝 | #5 / #2 | **校验仍然有效，并在重跑中继续受它保护**。按 §2.1 决策，重跑批次的预测与 ADS **同源生成**，站点集合天然一致；任何**异源**批次（自测数据、旧批次）仍不要拿去合 |
| D10 | 同库两套清洗口径 | `ads_quality_table` 的 8 站 / 287 桩 / 112,422 单来自 `export_ads_db.py` 的 Python 旁路复算，业务表是 PRL 清洗的 7 站 / 251 桩 / 97,804 单，两者并存于同一 `ads.db` | #3 / #4 | `ads_meta.cleaningSource` 已标注为过渡口径；统一后抽验表备注④可撤 |
| D11 | R02「重复记录」检出 0 —— **是注入端 bug，不是检测缺口** | 注入 11,200 条、检出 0 且**误报 128,817 条**。根因：生成器注入重复行时 `if key not in ("_row_id", "id")` 跳过了业务主键，于是"重复行"的主键各不相同；而检测端按契约 `primary_key` 分组，永远命中不到 | #2 / #4 | **上游已修复**（`feat/part2-02` 的 `d80bb60`）：口径拍板为「按业务主键判重」（`contracts/Q2-duplicate-policy-decision.md`），生成器改为连业务主键一起复制，策略文件 `policy_version` 0.1.0→0.2.0-draft，检测端无需改。**本演示批次是修复前生成的，故大屏上 R02 仍为 0** |
| D15 | R04「时间格式混杂」检出 0 —— **归因已更正：非 PRL 检测缺口** | 旧批次证据记「注入 11,200 / 检出 0」。逐层核查（2026-09-15）：注入端正常（`ods_orders.reserved_at` 1,200 + `ods_telemetry.recorded_at` 10,000，斜杠与 Unix 各半，脏值完整落盘）；**PRL 检测端自 `baf3b2d` 起即按契约判定**，集成机实跑得 `Q4 TP=11,200 / FP=0 / FN=0`（召回 1.0） | #2（归因）/ #4（ADS 侧） | ✅ **已闭环**：真缺口在 **ADS 侧独立复算**（此前仅「不可解析」才计，致大屏恒 0）——新增 `_lib.is_nonstandard_time()` 并在 orders / station_hourly / telemetry / events 四处对齐（**清洗宽容度不变**，仍仅不可解析剔除）；策略文件新增 `time_format_policy`（升至 `0.2.1-draft`）、测试 `scml/tests/test_quality_r04.py`（6 例）。**重跑后报告与大屏均应显示 11,200 / 召回 1.0** |
| D14 | 「平均等待时长」恒为 0，且三处文档把原因写错了 | #2 在 `feat/part2-02` 的 `6524503` 查明：DWD 的时间列是 **STRING**，Spark 3.5.7 上直接 `UNIX_TIMESTAMP(字符串)` 返回 `null`，相减与 `AVG` 静默变成 0（不是"ODS 未建模"）。同时发现三处解释都是错的：`part2/scml/warehouse/README.md:210`、`server/README.md` 的 `avgWaitNote` 行、以及 `ads.db` 里 `ads_meta.avgWaitNote`（由 `export_ads_db.py` 写入） | #2 / #4 | ✅ **已闭环，并更正最终结论**：SQL 已修（显式 `CAST(... AS TIMESTAMP)`）、三处解释已全部更正（`warehouse/README.md`、`server/README.md:138`、`_ads_extras.py` 的写入文案）；**但 2026-09-15 重跑后该指标仍是 0 —— 真实原因是数据特征**：生成器对 `reserved_at` 与 `started_at` 写入**同一时间戳**（`data_generator/generator.py:486-487`），重跑批次实测 **110,696 / 110,696 条已完成订单两者完全相等**，且 `ads_etl.sql` 的 `WHERE started > reserved` 会过滤全部等值行。**要让该值非 0 须改生成器并重跑**（后续批次议题）；**答辩口径按「生成器未建模预约等待时段」说明，不要再表述为 SQL bug** |
| D16 | Q9 改「置空坐标」后，两处必填断言未同步 —— PRL 连续失败两轮 | Q9 由「隔离越界站点」改为「**仅置空坐标（repair）**」后：① `assert_dwd`（业务约束）因 `latitude/longitude` 为 NULL 报 `dim_stations` 1 violation；② 修好①后 `assert_scml`（SCML **写后读回**验收）仍报同一处——**两处用的是各自的 optional 配置**（`quality-policy` vs `scml-dwd` 契约），需分别声明坐标可空 | #2 | ✅ **已闭环（2026-09-15）**：`quality-policy-v0.1.json` 的 `optional.stations` 与 `scml-dwd-v0.1.json` 的 `dim_stations.optional` 均已增列经纬度（前者版本升至 `0.2.2-draft`），并在 `contracts/README.md` §7 第 4 条补「断言同步」说明。**DWD 数据本身正确**（8 站 / 115,796 单 / 859,727 遥测 / 17,228 小时），断言只在写后读回环节拦截；`coord_imputed` 恒为 0 不触发 |

## 2.1 上游修复与演示批次的时间差（**已拍板：重跑 + 演示机迁址**）

`feat/part2-02` 里有两处**会改变重跑结果**的修复：Q2 判重口径（D11）与 `avg_wait` 的 CAST（D14）。
它们对旧演示数据的影响是：

| 项 | 旧演示批次（09-15 15:00 物化，修复前） | 重跑后（预期） |
|---|---|---|
| 质量面板 R02 | 注入 11,200 / 检出 **0**（注入端 bug，另误报 128,817） | 检出**非 0**（判重口径对齐后） |
| 质量面板 R04 | 注入 11,200 / 检出 **0** | 检出**非 0**（ADS 侧计数口径已修复，见 D15） |
| 区政府页 平均等待 | 恒 **0** | **非 0**（CAST 修好） |
| 订单/营收/桩数等业务指标 | 97,804 单 / ¥3,900,477.25 / 7 站 251 桩 | 待重跑确认（判重口径变了，清洗结果可能变） |

**决策（2026-09-15 晚，#2 TL 拍板）——选「在演示机重跑整链路」，并同时落地三件事：**

1. **演示机迁址**：团队演示机由 #1 的 `TimeMachine`（192.168.88.131）迁至 #2 的 `niyujun01`（192.168.59.128，VMware NAT），#2 已通知 #1。
   地址类材料已同步切换（答辩步骤、指标清单、E2E 与截图脚本默认值）；迁址前的截图与抽验真值按**历史批次**保留原样，避免伪造记录。
2. **ML 不取 #5 的 `forecast.db`**：重跑后 ODS/DWD 内容变化，会使 #5 那份预测批次失去配套关系（`merge_ads.py` 硬校验站点集合与源批次）。
   故按 #5 的四步机制（`verify_coverage` 门禁 → `train` → `predict` → `publish`）在 `niyujun01` 上**代 #5 重跑**，DWS/ADS 构建时带 `FORECAST_HANDOFF` **一次成型**（规避 D8 重演）。
   #5 已完成的批次与其 Ubuntu 22.04 复验记录（`MODEL_REPORT.md` §12.5）作为**方法可在目标机复现的独立证据**保留，不随重跑作废。
3. **R04 归因更正**：见 D15 ——「检出 0」是旧批次结论；PRL 侧代码早已按契约判定（集成机实跑 **Q4 TP=11,200 / FP=0 / FN=0，召回 1.0**），真缺口在 ADS 侧独立复算的计数口径，已修复并补测试。

> 配套动作：重跑完成后重填抽验表、重刷五页截图与答辩材料，并把本文 D8/D9/D11/D14/D15 的现状逐条更新为新批次数字。
| D12 | 生成器注册时间全在业务窗口之前 | 导致 `new_user_cnt` 全 0（见 D6） | #4 | 在窗口内注入注册时间并重新交接 ODS，或固定「首单新客」口径 |

## 3. 需要留意、尚未复现的风险

| # | 风险 | 说明 |
|---|---|---|
| R1 | `server/services/ads_reader.py` 的默认库位置改了 | #4 的分支 `feat/part2_SCML`（`abef18e`）把默认库从 `handoff/ads/ads.db` 改成**优先** `part2/scml/handoff/ads/ads.db`（存在即用）。合并后如果仓库里残留一个旧库文件，演示会**静默读错库**。合并时需要：确认没有残留旧库，或在启动脚本里显式设置 `ADS_DB` |
| R2 | 组内记录与实际不符（已澄清） | 此前记录写「PR #16 被关闭但没合并」，实际它已合入 dev（`merged=true`，merge_commit `a2bbb00`，head `657d4c0`）；未合入的是它之后的 4 个提交。已在本轮重新提 PR（#18） |
