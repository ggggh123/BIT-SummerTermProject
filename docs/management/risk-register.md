# 风险登记表

> 维护人：#1 王浩恩（PM）｜版本：v2｜更新：2026-09-05
>
> 风险优先级以核心交付为准。P0 未清零时不得以已提交、已构建或可选模块的测试通过替代真实核心联调证据。

| # | 风险 | 优先级 / 影响 | 预防与验收证据 | 降级 / 升级 | 责任人 | 状态（9/5） |
|---|---|---|---|---|---|---|
| R1 | 腾讯 Key、白名单、网络或 Qt WebEngine 异常 | **P0**：用户端地址解析与驾车/步行导航不可验收 | Key 配置化；最小加载、地址解析、QWebEngineView 两种路线分别冒烟 | 显示明确外部服务错误与最后成功路线；地图核心要求仍须修复，不可删除 | #3 | **开放** |
| R2 | Qt WebEngine/Charts 构建失败 | **P0**：管理端图表/用户端地图受阻 | 在 Linux 原生目录完成完整构建；分别记录 WebEngine、Charts 与核心目标结果 | 图表可降级为表格；地图属 P0，必须修复不可删除 | #2/#3 | **开放** |
| R3 | TCP 协议变化导致多人阻塞 | **P0**：核心联调停滞 | 冻结合同、共享测试向量并只允许向后兼容新增 | 回退冻结合同；不以可选接口改动影响核心链路 | #2 | **开放** |
| R4 | SQLite 并发冲突 | **P0**：数据不一致 | 服务端唯一在线写入、WAL、串行事务和事务边界测试 | 重启服务端并恢复最后成功数据库，禁止直接改库 | #2/#4 | **开放** |
| R5 | ML 环境、训练、在线发布或 active forecast 缺失 | Optional 展示风险：不影响核心发布 | 保留离线管线、独立 pytest、模型/指标工件与兼容测试 | 用户端显示“暂无预测”；找站、导航、充电继续可用 | #5/#3 | **可选，开放** |
| R6 | Web snapshot、静态 HTTP 或轮询失败 | Optional 展示风险：不影响核心发布 | 保留独立 Node 测试和手动静态演示说明 | 显示 stale/最后成功数据；不进入 core gate | #1 | **可选，开放** |
| R7 | 现场版本漂移或非同提交彩排 | **P0**：验收证据失效 | 两次彩排均记录 SHA、环境、黄金库 hash、日志和结果；锁定通过包 | 只使用最后一个同提交双彩排通过包 | #4/#1 | **开放** |
| R8 | 演示数据被污染 | **P0**：数据矛盾、答辩事故 | 固定 seed、黄金库、manifest/hash 和复位脚本 | 重新执行复位，禁止现场手改 SQLite | #4 | **开放** |
| R9 | 服务端协议 action 未完整实现，只有 5/27 action 的已核验实现 | **P0 / 最高**：用户端、管理端和模拟器无法形成闭环 | 对照冻结合同补齐核心 action；为每类成功/失败路径补专属测试；记录请求与响应 | 未补齐即 NO-GO；停止合入和现场发布 | #2 | **开放** |
| R10 | 服务端预约—充电—遥测—停止—结算状态机或串行写库缺失 | **P0 / 最高**：订单、余额、统计可能不一致 | 状态转移测试、事务边界测试和同库一致性检查；禁止客户端/模拟器直写运行库 | 回退到最后可复现提交；有数据矛盾即停止彩排 | #2/#4 | **开放** |
| R11 | 服务端未纳入根构建 target 或缺少专属测试，提交不能证明可发布 | **P0 / 最高**：无法获得可复现构建与回归证据 | Linux 原生目录完整 CMake build、CTest、服务端专属测试和启动证据 | 验证失败即不可合入；不得将共享协议测试当作服务端测试 | #2 | **开放** |
| R12 | 黄金库 hash 或模拟器状态/遥测/故障的真实服务端集成证据缺失 | **P0**：数据一致性和两次彩排证据失效 | golden manifest/hash 校验；在真实服务端比对状态、遥测、故障事件、订单、余额与统计；另录双实例/双路径完整日志 | 重置黄金库，禁止手改；真实服务端接入或双实例/双路径未证明时即停止彩排 | #4/#2 | **开放** |
| R13 | `SimulatorClient::sendStatus()` 固定发送 `state=running`，不反映 Pause/停止状态 | **P1**：服务端、用户端与管理端可观测到错误运行态 | 改为从模拟器当前状态生成上报值。**关闭证据**：针对性 QtTest 分别捕获并断言 running/paused/stopped 载荷，且真实服务端日志证明 Pause→Resume→Stop 转移与模拟器一致 | 证据不全时保持开放；不得用 `R12` 集成证据或现有 13/13 CTest 代替针对性回归 | #4 | **开放** |
| R14 | fault/recovery 复用未推进的 `currentTime_`，导致 `recordedAt` 可能不严格递增 | **P1**：共享 device-event cursor 可遗漏、重复或错序处理故障/恢复事件 | 为每个事件分配严格递增的 `recordedAt`，必要时以单调序列进位。**关闭证据**：针对性单元测试覆盖同一 tick 内连续 fault→recovery 及其与遥测交错，逐条断言 `recordedAt` 大于前一事件；真实服务端 cursor 日志证明无事件丢失或重复 | 证据不全时保持开放；不得仅以人工等待不同时刻或单次截图规避 | #4 | **开放** |
| R15 | 用户端会话 guard/race 覆盖与关闭证据不足，旧会话回调或迟到响应可在重登录/切换账号后继续生效 | **P1**：旧用户数据可污染新会话，或错误推动页面前进/跳转 | 请求、响应和异步回调均绑定不可变会话 generation/token，会话失效或切换时取消旧请求并清理页面状态。**关闭证据**：可重复自动化测试在会话失效后重登录、快速切换账号和响应乱序场景主动注入旧响应，断言新会话模型/界面不变、无旧用户数据且页面未被推进；真实服务端重放日志与结果一致 | 证据不全时保持开放；发现跨会话污染立即停止用户端彩排 | #3 | **开放** |

## 升级规则

- R1–R4、R7–R12 任一触发：PM 当日站会通报，立即阻断 core gate；超过半天未恢复则升级全队集成检查。
- R13–R15 按 P1 排期并保持开放，直到各自针对性回归和真实服务端证据均可复核；不得用通用 CTest、页面截图或 `R12` 的集成限制代替具体代码风险。
- R5–R6 只影响可选加分演示；不得挤占 P0 风险的人力、排期或发布判断。
