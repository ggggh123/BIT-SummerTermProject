# 远端 dev 发布前审计（2026-09-08）

审计对象：`ggggh123/BIT-SummerTermProject`。通过实际 `git fetch origin --prune`、提交／目录比较及 GitHub API 查询核对；不是依据旧本地跟踪引用推断。所有状态均注明检查时点，后续 PR 与 CI 状态以 GitHub 为准。

## 1. 检查开始时的远端状态

| 项目 | 观察结果 |
|---|---|
| `dev` | `a15e0881c927a24d775033f2ae35ceb16c8049cc`；北京时间 2026-09-08 15:35:06 合入 PR #12 |
| `main` | `22b146cc18b185931a8d4d91f9507e390e566311`；仍为较早里程碑，不包含这批新版 UI |
| 分支关系 | `main` 是 `dev` 的祖先；`main...dev` 为 0 / 85 个提交，包含历史与合并提交，不等于 85 个新增功能 |
| PR | #11、#12 已合并；检查开始时没有其他打开的 PR |
| Ubuntu 22.04 CI | 更新前 `dev@a15e088` 的 [run 34200065885](https://github.com/ggggh123/BIT-SummerTermProject/actions/runs/34200065885) 成功；`7bbfc0e` 的 [run 34177000267](https://github.com/ggggh123/BIT-SummerTermProject/actions/runs/34177000267) 也成功 |
| 身份与权限 | CLI 登录为 `ggggh123`，本机 Git 作者为 `ggggh123`，使用账号对应的 GitHub noreply 邮箱 |
| 分支保护 | 查询时 dev/main 未启用保护。本轮不更改仓库权限，仍采用功能分支 PR，不利用直推权限绕过审查 |

## 2. 队友新增内容及处置

从本机 UI 起点 `7bbfc0e` 到最新 dev 的净变更为 16 个文件；不把 PR 中重新带入的全部历史提交重复算作本日开发量。

[PR #12](https://github.com/ggggh123/BIT-SummerTermProject/pull/12) 带来：

- 快速部署 `scripts/quickstart.py`、源码安装入口和打包脚本。
- 内置团队测试地图 Key、环境变量／INI 覆盖能力与相关配置测试。
- `demo_runtime_unit` 的慢虚拟机容错超时从 90 秒改为 240 秒。
- Ubuntu 22.04／Qt 6.2 兼容性、QSaveFile 诊断与团队指南更新。
- 概要设计 Word 文档及人工需求矩阵更新。

以上内容通过普通 merge 完整接入最终版分支。Word 文档、需求矩阵及黄金库与 `origin/dev` 比对保持一致；没有编辑矩阵、覆盖他人文件或重复封存数据。

## 3. 为什么没有回灌旧服务端分支

`origin/feat/server-health` 更新为 `f7fe64c`，但它的提交时间为 9 月 7 日，晚 fetch 不等于内容比当前 dev 更新。与已集成快照 `6360bd1` 比较，净差异只在 9 个路径，主要是缺少核心黄金库三件套、部分证据、QSaveFile 加固与诊断工具，以及一项模拟器测试差异；没有这次最终版必须额外引入的新管理功能。

该分支的历史不完全出现在 dev 祖先链上，是此前快照式整合造成的，不能据此判断整套服务端尚未交付。机械合并旧快照会有回退风险。本轮保留远端分支，不删除、不强推、不重复引入。

## 4. 发布前发现并补齐的交付问题

| 问题 | 整理措施 | 验证 |
|---|---|---|
| quickstart 默认构建全部测试，并按所有 CPU 核心并行，违背轻量部署口径 | 默认 `ubuntu22`，只编译三端；并行 2，可显式 `--jobs 1` 或 `--preset ubuntu22-test` | 独立替身测试检查 CMake 参数，不实际安装依赖 |
| 安装入口依赖调用者正好在包根目录 | 根据脚本自身位置定位完整源码根目录，兼容路径空格与仓库模板位置 | 从另一工作目录调用两种安装入口 |
| 打包黑名单过窄，会混入本机配置／缓存；只收核心 DB 与 hash，缺少 manifest | 先剪枝，排除本机目录、个人配置、活动 DB 和压缩包；保留黄金工件并核对核心 hash；拒绝外部链接 | 解包检查、缺件／错 hash／符号链接拒绝、重复生成不覆盖 |
| 顶层 README、开发指南和交接单仍指向旧分支／旧待办 | 增加最终版唯一入口、更新团队启动说明、历史记录加时点标识 | 对本轮文档与引用文件核对，不复写历史结论 |
| UI 回归日志被全局 `*.log` 忽略，推送后文档证据链接会失效 | 仅放行已审阅的指定文档证据目录；运行日志继续排除 | 提交清单和引用完整性检查 |

没有重新设计已验收 UI、修改接口结构或绕过业务权限；没有为修复环境问题升级系统或安装另一套 Qt。

## 5. 版本整理与边界

- `e716d16`：固定用户已通过的三端 UI、个人统计与反馈修订，保留可回溯的原始验收内容。
- `6bcbdb1`：普通 merge 整合远端 `dev@a15e088`，保留各队员历史和内容。
- 后续交付整理补充脚本回归与当前文档；最终提交以本次 PR HEAD 为准。
- 仅推送 `feat/energy-pulse-qt-ui` 并面向 dev 提 PR，不直接合并 dev→main、不改保护规则、不删除旧分支、不发布旧包为新版。
- 本机自动化、Ubuntu 22.04 CI、用户本机验收、其他虚拟机人工实测是不同证据，见[最终交付说明](../release/final-2026-09-08.md)与[证据索引](../test/evidence/final-2026-09-08/README.md)。
