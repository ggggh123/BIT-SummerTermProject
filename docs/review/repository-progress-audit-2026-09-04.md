# 仓库进展审计（2026-09-04）

> 审计范围：当前核心交付重置前的已验证仓库证据。结论用于排序 P0，不以代码量、提交数或合并状态代替集成完成度。
>
> 基准：`dev@04e45fcb4eb8ab5f14f526588e01d28fb4ffffd5`；不可变上游工作簿输入：`main@fc7c67ed076e220f7a28453b696241dcc6ad8f83`。

## 1. 证据层级定义

| 层级 | 含义 | 不能推导为 |
|---|---|---|
| 已提交 | 变更存在于给定 SHA/分支 | 可构建、可测试或可运行 |
| 已构建 | 指定环境的构建命令成功 | 业务行为正确或接口已联调 |
| 已测试 | 指定测试命令通过并有计数 | 程序已启动或多进程真实联调 |
| 已独立运行 | 单一模块/静态服务在限定场景运行 | 与真实服务端、数据库、模拟器的端到端闭环 |
| 已真实联调 | 同一提交上真实用户端、服务端、数据库/模拟器按合同完成观察到的业务链路 | 仅由提交、build、test 或独立运行不能证明 |

除明确写为“已真实联调”的事项外，以下记录都不能作为 9 月 10 日 core GO 证据。

## 2. 分支拓扑与 PR 状态

```text
origin/main @ fc7c67e  （当前不可变上游工作簿输入）
  └─ PR #5 已合并的 feat/data 端点 @ be9ce03
      └─ current feat/data @ d3d4abf（be9ce03 后续提交，尚未进入 main）

origin/dev  @ 04e45fc  （当前开发集成基准）
  └─ PR #4 已合并：feat/user -> dev

功能来源分支：
  feat/user   @ 56b1865
  feat/server @ 8380993
  feat/data   @ d3d4abf
  feat/web    @ 1d07976
  feat/ml     @ 2b1563d
```

| PR | 状态（审计时） | 含义 |
|---|---|---|
| [#4](https://github.com/ggggh123/BIT-SummerTermProject/pull/4) | 已合并，`feat/user -> dev`，2026-09-04 06:45:49Z | 用户端提交已进入 `dev`；尚不构成与真实服务端/模拟器闭环的证据 |
| [#5](https://github.com/ggggh123/BIT-SummerTermProject/pull/5) | 已合并，`feat/data@be9ce03 -> main`，2026-09-04 02:56:34Z | PR 合入端点 `be9ce03` 已进入 `main@fc7c67e`；当前 `feat/data@d3d4abf` 是其后续提交，**尚未进入 main**，且尚不构成完整核心运行的证据 |

## 3. 模块审计结果

| 模块/负责人 | 已验证证据 | 未能证明的事项与结论 |
|---|---|---|
| #2 Qt 管理/服务端 | 仅 5/27 action 有实现证据 | 根 CMake 未纳入服务端 target、无服务端专属测试；协议/状态机/测试 P0 未闭合，**暂不可合入，且是 core NO-GO** |
| #4 SQLite/模拟器 | 数据库 9/9 项完成；核心 CTest 13/13；黄金库完整性有证据 | 两个模拟器风险仍开放：模拟器状态/遥测/故障事件与真实服务端的端到端接入未被证明，且双实例/双路径现场行为不可由数据库测试推导 |
| #1 Web | 36/36 测试通过；静态 HTTP 入口返回 200 | 未完成真实 snapshot writer/真实服务端快照联调；按范围 v2 仅为 Optional，不能作为 core gate 证据 |
| #5 ML | 78 passed / 3 skipped；离线管线可从 12,960 行历史生成 144 条预测 | 未真实发布到服务端；按范围 v2 为 Optional，active forecast 不属于 core 前置条件 |
| #3 Qt 用户端 | PR #4 已合并到 `dev`，用户端闭环与腾讯地图导航为核心实施来源 | 未以当前服务端、黄金库和模拟器在同一提交上证明完整真实联调；不得只以 PR 合并作验收结论 |

## 4. 当前 P0 与 P1

### P0（阻断核心 GO/NO-GO）

1. #2 补齐服务端核心协议 action，并以专属测试证明成功、失败和权限/状态边界。
2. #2/#4 实现并验证预约→充电→遥测→停止→结算状态机、串行事务、余额/订单/统计一致性。
3. #2 将服务端纳入可复现构建与测试证据，解决 root target 与专属测试缺口。
4. #3 验证登录、查站/查桩、腾讯地址解析和 QWebEngineView 驾车/步行路线，并在核心闭环中接入。
5. #4 验证黄金库 hash、模拟器状态/遥测/故障与服务端的真实交互；在同一提交完成两次彩排。

### P1（不阻断核心发布）

- Web 的真实快照 writer、静态演示完善、轮询/展示优化。
- ML 真实服务端发布、在线预测与指标展示。
- 仅提高观感的动画、复杂筛选、附加图表和模型演示。

## 5. 建议合并与验证顺序

1. 先在当前 `dev@04e45fc` 同步服务端工作并解决 #2 的 P0；未满足 root 构建与专属测试前不请求合入。
2. 在该同一集成提交上接入 #4 黄金库/模拟器和 #3 用户端，跑核心 build、CTest、数据库校验及端到端记录。
3. 固定候选 SHA，连续两次从受校验黄金库彩排；两次均通过且无开放 P0 后，才进入 `main` 的稳定里程碑审查。
4. 最后以独立 PR 选择性归档 Web/ML 已验证成果，使用 explicit optional profile，不回退当前文档、工作簿、合同或核心基线。

## 6. 审计结论

截至 2026-09-04，数据库、Web、ML 和用户端存在不同层级的提交/测试或独立运行证据，但**核心真实端到端闭环未被证明**。服务端协议、状态机、构建与专属测试缺口是最高优先级；在其关闭并取得同一提交双彩排证据前，核心发布结论为 **NO-GO**。
