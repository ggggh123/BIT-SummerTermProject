# 2026-09-15 证据：跨机演示态截图 + 抽验真值

> 原采集人：#1 王浩恩（PM）｜**本轮重采：#2（2026-09-15 深夜，全链路重跑后）**
> **当前数据源（官方口径，重跑批次）**：`handoff/ads/ads.db`，`runId=ads-20260915235603`，**8 站 / 287 桩 / 110,696 单 / 90 天**（由 PRL 清洗后的 DWD 经 SparkSQL 链路产出）
> **预测**：真实 MLlib 批次 `f-20260915-232113`（`gbt-3.5.7`，6 站 × 24 点，`is_baseline=0`，24/24 步长优于基线）
> 采集方式：VM（`niyujun01`，192.168.59.128）上 Flask 单进程托管 `web/dist` + `/api/*`；宿主机用系统 Edge 执行 `$env:PART2_CHANNEL='msedge'; node scripts/part2/62-capture-pages.mjs --out <本目录>/screenshots` 逐页截图**并同时导出图表真实 option**
> 运行细节与脚本适配缺陷见同目录 [`vm-integration-run.md`](vm-integration-run.md)
>
> 🔄 **批次沿革（两批不要混用）**：
> - **09-15 白天**（#1 采）：`runId=ads-20260915145956`，7 站 / 251 桩 / 97,804 单，演示机 `TimeMachine`（192.168.88.131）→ 截图归档在 **`screenshots-pre-rerun-20260915/`**
> - **09-15 深夜**（#2 重采）：全链路重跑后，8 站 / 287 桩 / 110,696 单，演示机 **`niyujun01`（192.168.59.128，VMware NAT）** → **本文 §1 截图与 §2 抽验真值为该批次**（团队演示机已于 09-15 晚由 `TimeMachine` 迁至 `niyujun01`）
>
> ⚠️ 本文 §3–§9 按时间顺序记录 2026-09-15 一整天的工作，其中 **§4 及 §3 里出现的是当天上午的本地 Python 物化批**
> （8 站 / 287 桩 / 112,422 单），与本文 §1/§2 的重跑批次不是一套数据。以 §2、§9 与
> `docs/management/part2-metric-checklist.md` 为准。

## 1. 截图

> **本轮（重跑批次）采集**：五页 1920×1080 PNG + 每页图表真实 option（JSON），由 `62-capture-pages.mjs`
> 于 2026-09-15 深夜生成；**五页共 20 个图表全部取证成功、控制台 0 报错**。
> **历史批次存档**：`screenshots-pre-rerun-20260915/`（7 站 / 251 桩 / 97,804 单，勿混用）。

| 文件 | 页面 | 路由 |
|---|---|---|
| `screenshots/01-home.png` | 综合运营大屏（主页） | `/` |
| `screenshots/02-user.png` | 用户视角 | `/#/user` |
| `screenshots/03-station.png` | 充电站视角 | `/#/station` |
| `screenshots/04-enterprise.png` | 企业视角 | `/#/enterprise` |
| `screenshots/05-gov.png` | 政府视角 | `/#/gov` |

## 2. 抽验真值（官方 PRL 口径）

> ✅ **本轮（重跑批次）的 22 项抽验真值以 `docs/management/part2-metric-checklist.md` 为准**
> （已于 2026-09-15 深夜整体重采：8 站 / 287 桩 / 110,696 单，含真实 MLlib 预测批次与五页 20 图 option）。
> 以下是**白天批次**的原真值，保留作为历史记录。

**权威表已迁到 `docs/management/part2-metric-checklist.md`（22 行、三段取值、含口径差异说明）**，
本节只留摘要，避免两处各写一份又互相漂移。下面数值由 `python scripts/part2/63-ads-truth.py` 直接查 `ads.db` 得到，
「与页面一致」一列由 `node scripts/part2/62-capture-pages.mjs` 取页面里 ECharts 实例的真实 option 比对。

| # | 指标 | ADS 来源 | 真值（官方 PRL 批） | 与页面一致 |
|---|---|---|---|---|
| 1 | 累计营收 | `ads_daily.revenue_fen` 合计 | **390,047,725 分 = ¥3,900,477.25** | ✓（主页 KPI） |
| 3 | 累计订单 | `ads_daily.order_cnt` 合计 | **97,804** | ✓（主页 KPI） |
| 4 | 桩在线率 | `ads_station` 五状态列 | **(251−1)/251 = 99.60%** | ✓（主页 KPI） |
| 5 | 空闲桩 | `ads_station.idle_cnt` 合计 | **175 / 总桩 251** | ✓（主页 KPI） |
| 6 | 桩状态五类之和 | `ads_station` 五个计数列 | **175 + 52 + 23 + 1 + 0 = 251**（= 总桩数） | ✓（主页环图五扇区） |
| 9 | 近 30 日营收合计 | `ads_daily` 末 30 日 | **129,632,987 分 = ¥1,296,329.87**（订单 32,665，客单价 ¥39.69） | ✓（企业页 KPI） |
| 12 | 月度营收 | `ads_daily` 按自然月 | 06 ¥607,700.30｜07 ¥1,346,957.17｜08 ¥1,323,553.67｜09 ¥622,266.11 | ✓（企业页月度表 4 行） |
| 13 | 用户 RFM 分层 | `ads_user_rfm.user_cnt` | 一般挽留 1,485｜一般发展 841｜重要保持 724｜重要价值 605 | ✓（企业页 RFM） |
| 17 | 等效碳减排 | `ads_district.co2_saved_kg` 合计 | **1,992,846.5 kg = 1,992.8 tCO₂**（电量 × 0.581） | ✓（政府页 KPI） |
| 20 | 全城峰值负荷 | `ads_station_hourly` 按小时合计 | **当日 7,153.3 kW（2026-09-14 08 时）**；窗口内最大 7,964.665 kW（2026-07-10 18 时） | ✓（政府页 KPI 已标注「当日」口径） |
| 21 | 数据质量检出 | `ads_quality_issue` | **注入 32,775 / 检出 10,717**（逐类见下） | ✓（主页数据质量面板） |

第 21 行逐类（`rule / 注入 / 检出 / 召回率`）：

> **预测三表的现状**：本机 `handoff/ads/ads.db` 目前是 seasonal-naive 基线（`is_baseline=1`，120 点 / 5 站）。
> 真实 MLlib 批次已由 #5 在其目标机跑出并合并（`part2/ml/MODEL_REPORT.md` §12.5：5 站 / 120 点 /
> `is_baseline=0` / `wape<baseline_wape` 24/24，负荷 test MAE 1h 48.60），**待把 `handoff/forecast/forecast.db`
> 交到演示机后用 `part2/ml/merge_ads.py` 接回**，届时本表第 22 行与主页预测曲线一并刷新。

```text
R01 缺失值      5600 / 5652 / 1.000     R06 金额口径错误   600 /  581 / 0.968
R02 重复记录   11200 /    0 / 0.000     R07 孤儿引用       360 /  684 / 1.000
R03 异常值      3360 / 3345 / 0.996     R08 非法字段值      16 /   16 / 1.000
R04 时间格式混杂 11200 /    0 / 0.000     R09 经纬度越界       1 /    1 / 1.000
R05 逻辑矛盾     412 /  412 / 1.000     R10 文本脏数据      26 /   26 / 1.000
```

## 3. 本轮暴露、需要他人闭环的问题

**3.1（#3 PRL）R02「重复记录」与 R04「时间格式混杂」注入 11,200 条、检出 0 条。** 这两类占注入总量（32,775）的 68%，召回率直接写 0 —— 答辩若被追问「十类问题都检出了吗」，这是硬伤。要么补齐检测规则，要么在报告里如实说明「ADS 侧口径不含这两类、由 #3 报告为准」，二者选一。

> **更正（2026-09-15 晚，见 §9.4）**：当时把两类都归为"检测规则缺口"，**其中 R02 归错了**。
> #2 后来查明 R02 是**注入端 bug**：生成器注入重复行时跳过了业务主键，导致"重复行"主键各不相同，
> 检测端按契约 `primary_key` 分组永远命中不到（并伴随 128,817 条误报）。口径已由 #2 拍板为
> 「按业务主键判重」并同步改了生成器与策略文件。**R04 仍是检测缺口**，未闭环。

**3.2（#4 SCML）正式规模下桩状态快照几乎全是 `idle`。** `reserved/charging` 均为 0、`fault` 仅 2 → 在线率 99.3%、空闲 285/287；主页「桩状态环图」只有一个扇区，且 11 项「各站空闲桩」全部并列 36，图表没有区分度。建议生成器按站点利用率生成状态快照（与「利用率」「故障率」两图自洽）。

**3.3（#2 TL）一键脚本的跨机适配**（`part2/scripts/start_part2.sh`）：`PART2_ROOT` 写死 `/home/bit/part2`、`sudo -u hadoop`、依赖 `curl`、用系统 `python3`。本机（`spiderboy` / `/mnt/hgfs/...` / 无 curl / flask 在 venv）三处断点，详见 `vm-integration-run.md` §2 与 §4 的补丁建议。

**3.4（#4 SCML）`/etc/profile.d/ev-second-project.sh` 在本机不存在**，而 `part2/scml/scripts/*.sh` 会 source 它 → SparkSQL 链路在本机跑不起来（本机是 `part2-env.sh`）。

## 4. 状态快照修复验证（#4 `6cbbd11`，2026-09-15 10:12 复测）

#4 在 `6cbbd11` 里按站点利用率重塑了桩状态快照。重新生成 ODS + 物化 ADS 后（`runId=ads-20260915101224`）复测：

| 项 | 修复前 | 修复后 |
|---|---|---|
| 桩状态分布（287 桩） | `idle 285 / fault 2` | **`idle 197 / charging 61 / reserved 27 / fault 2`** |
| 主页「桩状态环图」 | 只有 1 个扇区 | **4 个扇区** |
| 各站空闲桩 | 8 站里 7 站并列 36 | **30 / 29 / 26 / 26 / 24 / 22 / 21 / 19**（有区分度） |
| 主页 KPI | — | `空闲桩 197`、订单 112,422、营收 ¥4,758,207.37（金额与订单未变，仅状态分布变化） |

同一轮修复还包含：`reconcile.py` 不再硬要求 `is_baseline=1`（真实 ML 批次可过）、`build_local.py` / `export_ads_db.py` 新增 `--forecast-handoff <path>` 用于接入 #5 的预测包、`test_delivery_check.py` 子进程输出加 `errors="replace"`（已验证：**不设 `PYTHONIOENCODING` 也 3/3 通过**）。

> 残余项：`onlineRate` 仍是 99.3%，因为故障桩仍只有 2 个——若答辩希望在线率更接近真实运营，需要 #4 调整故障注入比例（与状态快照是两件事）。

## 5. 补页面级 E2E 时发现的两个版面缺陷（#1，2026-09-15）

**5.1 主页内容被静默裁切（已修）**：`ScaleFrame` 原本把宿主高度固定为 `1080 × scale`，而 `.scale-host` 是 `overflow: hidden`；实测主页内容高约 **1632 设计 px**（1080 之外的是地图下半部、数据质量面板、事件流）。在 1920×1080 投屏上，`.scale-host` 可视高 960px，`scrollHeight` 1451px → **约 491px 被裁掉**，地图只露出 111px，数据质量面板整块不可见（而它正是老师要求里的展示项）。

修法：宿主高度改为按**实际内容高度**计算（`contentHeight * scale`）+ `.scale-host` 改 `overflow: visible`，并挂 `ResizeObserver` 跟随内容变化。**随后在 `feat/part2-web-layout` 里做了彻底方案（见 §7）**：主页重排为三列大屏栅格，内容压到 1080 设计高度以内。

**5.2 主页地图面板里的北京底图过小、8 个站点重叠**：面板只有 300px 高（601×300 画布），geo 受宽高比限制只能占约 **260px 宽**，站点像素集中在约 54×38 px 内 → 环图之外的这 8 个点挤成一团，像素级点击无法区分站点（实测点上 1 号站的位置命中到 8 号站）。

已做的缓解：给 `geo` 补 `layoutCenter/layoutSize`，`symbolSize` 上限由 30 压到 16。**根治同样依赖 5.1 的版面重构**（把地图放进主视区、拿到更大高度）。

E2E 落地：`web/e2e/dashboard.spec.mjs`（条数见 §7），`npm run test:e2e` 全绿（主页渲染、地图点击跳转并选中同一站、5s 轮询重绘、接口失败保留旧数据、DataV 渲染、版面）。

## 6. 补老师"开发环境/要求"的差距（2026-09-15 11:00）

老师 6 条要求逐条核对后，本轮补了三处：

**6.1 正式规模数据入 HDFS（第 2 条）**：用 #4 的 `hdfs_put_ods.sh` 把正式规模 ODS 推到 `/ev-charging/ods` —— **104.7 MB / 360 个 `dt=` 分区**，含 `manifest.json`、`injection_log.json`、`_SUCCESS`（脚本会先清空目标，昨天那批 1/10 规模扁平 CSV 已被清掉）；并把 `handoff/dws` 四个汇总表推到 `/ev-charging/dws`（3.9 MB + manifest + `_SUCCESS`）。`/ev-charging/dwd`、`/ads` 仍是 9-14 的旧数据，等官方 SparkSQL on YARN 路线（阻塞在 #3 的 `handoff/dwd`）。

**6.2 维度与对比分析对照表（第 3 条）**：新增 `docs/management/part2-analysis-dimensions.md` —— **15 个分析维度**（要求 ≥8）与 **5 组维度对比分析**（要求 ≥2：快充↔慢充、区↔区、站↔站、时段↔时段、用户分层↔分层），每行都给「来源表 → Flask 接口 → 大屏位置」，答辩可直接照念。同文档附版本基线表（Python 3.11.16 / Spark 3.5.7 / Hadoop 3.4.1 / Flask 3.1.3 / Node 24.15.0 / Vue 3.5.13）。

**6.3 DataV 大屏件（第 5 条）**：接入 `@kjgl77/datav-vue3@1.7.4`（DataV 的 Vue3 版，`web/package.json` 已声明 `engines.node >= 23`）。主页大屏：5 个图表面板换 `dv-border-box-8` 边框（封装 `DvFrame.vue`）、标题与 KPI 行加 `dv-decoration-10`、实时事件流换 **`dv-scroll-board`** 滚动榜单。E2E 从 4 条扩到 **5 条**（新增「DataV 大屏件已渲染」：断言边框盒 ≥5 且尺寸非零、滚动榜单存在且高度 >100px），`npm run test:e2e` **5/5 通过**；五页截图已按新样式刷新。

## 7. 主页版面重构（`feat/part2-web-layout`，2026-09-15）

按《01-PM》§3.1 的三列大屏栅格重排主页，目标是"1920×1080 一屏放下 + 地图进主视区中央"：

| 区域 | 内容 | 高度（设计 px） |
|---|---|---|
| 第 1 行 | KPI 四卡 | ~96 |
| 主视区（左 1.05fr / 中 1.6fr / 右 1.15fr） | 左：近 30 日营收趋势 + 桩状态（各 150 图表）｜**中：北京市站点分布（图表 380，列最宽）**｜右：利用率排行（150）+ 数据质量（列表内滚动 132） | ~450 |
| 底行（1.7fr / 1fr） | 24 小时负荷与预测（图表 220）｜实时事件流 ScrollBoard（220） | ~290 |

同时修了 `ScaleFrame` 的预留高度：原来固定预留 120px，而顶栏+页脚实际约 175px，导致整页比视口高约 55px、仍要滚一下；现在按**真实占位**（宿主顶部 + 页脚高度 + 12）预留。

实测（`web/e2e/dashboard.spec.mjs` 第 6 条，打 VM 真实栈）：`.scale-inner` 内容高 **≤1080 设计 px**、地图图表高 **≥380px**、整页 `scrollHeight ≤ 视口 + 32px`。E2E 由 5 条扩到 **6 条，全部通过**；五页截图已按新版面刷新（`screenshots/01-home.png` 由 203KB 变 266KB，内容更密）。

## 8. DataV 铺到 4 个视角子页（2026-09-15）

老师第 5 条要求"使用 DataV 进行大屏展示"，上一轮只做了主页。本轮把 `DvFrame`（`dv-border-box-8` + `dv-decoration-10`）铺到 `UserView`/`StationView`/`EnterpriseView`/`GovView` 的所有面板（含月度汇总表、各区服务指标表等非图表面板）；带控件的标题（站点选择器、7/30 日切换）保留为 `.panel-heading` + `.panel-heading-extra`，与边框共存。子页**不套 `ScaleFrame`**——设计上子页是响应式看板，只有主页做大屏等比缩放。

新增 E2E 第 7 条「四个视角子页统一使用 DataV 大屏件，且渲染无报错」：逐页断言图表数（用户 4 / 充电站 4 / 企业 4 / 政府 3）、DataV 边框 ≥3、控制台零报错 → **E2E 7 条全部通过**。五页截图已再次刷新（子页体积从 229/183/238/202KB 变为 183/185/195/161KB，即观感与留白变化）。

## 9. 收尾：错误修复、口径标注、取证脚本固化成仓库脚本（2026-09-15 晚）

### 9.1 修掉两个答辩会看得见的错误

| 缺陷 | 现象 | 处理 |
|---|---|---|
| 主页「近 30 日营收趋势」实际只有 7 个点 | 接口清单里主页 `revenue-trend` 写死 `days=7`，视图再 `slice(-30)` 也只有 7 个点；图上 7 根柱子挂着「近 30 日」标题 | `web/src/api/endpoints.js` 改 `days=30`；单测对准页面真正调用的 `buildRevenueTrendOption30d`（断言标题/点数/横轴都是 30）；新增 E2E 第 8 条，从页面 ECharts 实例取 option 断言 30 个点 |
| 企业页「新增用户」看着像数据坏了 | `ads_daily.new_user_cnt` 整列 0，图上是贴地零线；真实口径是**窗口内完成首单的新客**（生成器 4,985 个用户注册时间全在窗口之前） | 图例与系列名改为「窗口内首单新客」，图下加口径说明；构造器从视图内联挪到 `src/lib/charts/enterprise.js` 的纯函数，便于单测 |

同时把「全城峰值负荷」的当日口径写进界面（`/api/gov/peak-load` 只返回单日 24 点，窗口内最大要高出约 10%）：
KPI 标题改「全城峰值负荷（当日）」，提示行给出具体日期与「非窗口峰值」。三处改动详见
`docs/management/part2-defect-log.md` 的 D1 / D5 / D6。

### 9.2 抽验与截图不再靠手工，四段证据变成可重跑命令

本目录的截图上一次是手工产物，抽验表里的图表数值靠人眼看图读数——既有误差也不可复现。
本轮把本机临时脚本转正到 `scripts/part2/`（详见 `scripts/part2/README.md`）：

| 脚本 | 对应抽验表的哪一段 |
|---|---|
| `63-ads-truth.py` | ADS 真值 |
| `61-dump-api.sh` | 接口值（27 个契约端点全量存档） |
| `62-capture-pages.mjs` | 大屏显示值 + 五页截图 |
| `60-status.sh` | 一键体检（集群 / 服务 / HDFS 分层 / `ads.db`） |

取「大屏显示值」不再看截图读数：`web/src/components/EChart.vue` 把 ECharts 实例挂在 `.chart` 容器的
`__echarts` 上（作者本来就留给 E2E），直接取 `getOption()` 就是图表**实际渲染的那组数**。
每个页面还会把该页所有图表的 option 与页面文本落成 `screenshots/NN-<page>.json`，作为可复查的存档。

本次刷新结果：五页全部取证成功、**控制台 0 报错**；截图与 JSON 均在本目录。

### 9.3 本轮测试状态

| 项 | 命令 | 结果 |
|---|---|---|
| 前端纯函数单测 | `node --test "web/tests/*.test.mjs"` | **37/37** 通过 |
| 第一阶段回归 | `node --test "dashboard/tests/*.test.mjs"` | **36/36** 通过 |
| 页面 E2E（打 VM 真实栈） | `npx playwright test` | **9/9** 通过 |

E2E 九条：主页 5 图表 + KPI 零报错、主页营收趋势确实是近 30 日、地图点击跳转并选中同站、
5s 轮询确实重绘、接口失败降级不白屏、DataV 边框件、主页 1920×1080 一屏放下、四个子页统一 DataV、
两处容易误读的口径在页面上写清楚了。

### 9.4 上游两处修复会改变「重跑之后」的结果（待团队拍板）

#2 的 `feat/part2-02` 里有两处**会改变重跑结果**的修复，都已在分支上落地但**未合入 dev**：

| 修复 | 内容 | 对重跑结果的影响 |
|---|---|---|
| Q2 判重口径（`d80bb60`） | 拍板「按业务主键判重」，生成器注入重复行时改为连业务主键一起复制，策略文件 `policy_version` 0.1.0→0.2.0-draft | 质量面板 R02 由「注入 11,200 / 检出 **0**」变为**检出非 0**；业务指标是否变化待重跑确认 |
| `avg_wait` 静默为 0（`6524503`） | DWD 时间列是 STRING，Spark 上直接 `UNIX_TIMESTAMP(字符串)` 返回 `null`，相减取平均静默变 0；改为显式 `CAST(... AS TIMESTAMP)` | 区政府页「平均等待」由恒 **0** 变为**非 0** |

**本演示批次（`runId=ads-20260915145956`）是修复前物化的**，所以上面两项在大屏上仍是旧值。
两处修复同时暴露出三处把原因写错的文档（`part2/scml/warehouse/README.md`、
`server/README.md` 的 `avgWaitNote`、`ads_meta.avgWaitNote`）需要一并更正。

于是现在只剩一个决策（**它与"ML 批次怎么进演示机"是同一个**）：

1. **保持当前批次**：抽验表、截图、答辩材料继续有效；答辩时把 R02=0 与平均等待=0 讲成"上游已定位并修复、本批是修复前产物"。ML 则合并 #5 交来的 `forecast.db`。
2. **在演示机重跑整链路**：数字与上游口径对齐，ML 也在本机顺手重跑（不必等 `forecast.db`），但抽验表、截图与答辩材料要整体重填。

完整对照见 `docs/management/part2-defect-log.md` §2.1。
