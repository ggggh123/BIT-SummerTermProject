# 08 · 第二阶段集成验证报告（#2 TL）

> 集成/演示机：`niyujun01`（Ubuntu 22.04.3 / Hadoop 3.4.1 / Spark 3.5.7）
> 集成日期：2026-09-14
> 依据：《02-TL-Hadoop平台与Flask后端设计》§4 集成职责
> 结论：**端到端链路已跑通，大屏可在 Windows 浏览器直接访问**

---

## 1. 集成范围与取用方式

从三个远程分支取用成果（**未做分支合并**，按目录取用，避免污染各人分支）：

| 来源分支 | 取用目录 | 内容 |
|---|---|---|
| `feat/part2_SCML` | `server/` | Flask 后端（27 条契约路由 + health） |
| `feat/part2_SCML` | `part2/scml/` | 生成器、数仓加工、对账、交接契约 |
| `feat/part2-web` | `web/` | Vue3 + Vite 前端工程 |
| `feat/part2-ml` | `part2/ml`、`part2/sql` | MLlib 预测子系统与分担两表 |

集成机上组织为仓库同构结构：`~/part2/{server,web,part2,dashboard,scripts,logs}`。

## 2. 端到端链路与验证证据

链路：`生成器 → handoff/ods → DWS/ADS → ads.db → Flask → web/dist`

### 2.1 数据层

| 环节 | 结果 |
|---|---|
| ODS 生成（正式规模） | 耗时 1 分 54 秒；`107 MB`；注入 **32,775** 条；`validate_handoff.py` **校验通过** |
| DWS/ADS 物化 | 耗时 1 分 20 秒；`dws 3.9 MB`、`ads.db 5.5 MB` |
| **三层对账** | **30 项：通过 29、失败 0、跳过 1** → `[OK] 对账全绿` |
| 跳过项说明 | D 组「DWD ↔ DWS」需 #3 的 `handoff/dwd`（尚未交付），按契约跳过 |
| 基线预测 | seasonal-naive WAPE **17.58%** vs 常量基线 67.63%（#5 ML 批次未交接，按《05》§8 降级并如实标注） |

ADS 库规模（`/api/health` 上报）：`ads_charger 287`、`ads_daily 90`、`ads_district 5`、`ads_event 20000`、`ads_forecast_24h 144`、`ads_forecast_metric 24`、`ads_quality_issue 10`、`ads_station 8`、`ads_station_hourly 17280`、`ads_user_rfm 8`。

### 2.2 服务层

| 环节 | 结果 |
|---|---|
| 契约自测 `selftest_contract.py` | **55 项全部通过，失败 0** |
| 前端结构自测 `selftest_frontend_shape.py` | **46 项结构性比对，不兼容 0 处** |
| 路由注册 | `apiRouteCount 28` / `contractEndpointCount 27` |
| 运行标识 | `runId = ads-20260914194754`；数据窗口 `2026-06-17 → 2026-09-14`（90 天） |

KPI 抽样（`/api/overview/kpis`）：总订单 **112,422**、总营收 **475,820,737 分**（475.82 万元）、总电量 **3,972,088.2 kWh**、桩数 287、在线率 **99.3%**。

### 2.3 前端与外部可达性

| 环节 | 结果 |
|---|---|
| 构建 | `npm ci` 62 包 / 18 秒；`npm run build` 成功，产物 **1.4 MB**（含本地 `geo/beijing.json`，不走 CDN） |
| Flask 托管大屏 | `GET /` → **HTTP 200** |
| 静态资源 | `GET /assets/index-*.js` → 200；`GET /geo/beijing.json` → 200 |
| **Windows 侧跨机访问** | `http://192.168.59.128:5000/` → **200**；`/api/overview/kpis` → **200**；`/geo/beijing.json` → 200 |

> 证明：大屏不依赖"必须在虚拟机里看"——**Windows 浏览器直接可访问**，答辩投影可脱离虚拟机。

## 3. 一键脚本（《02》§4.3 交付）

| 脚本 | 作用 | 实测 |
|---|---|---|
| `scripts/start_part2.sh` | HDFS/YARN → 数据检查 → Flask（含大屏托管）→ 输出访问地址 | ✅ 输出 `守护进程 5/5`、`ADS 库 OK`、`前端产物 OK`、`服务已就绪（runId=...）` |
| `scripts/stop_part2.sh` | Flask → YARN → HDFS（反向顺序，避免元数据损坏） | ✅ 语法与逻辑校验通过 |

脚本源码纳管于工作区 `part2/scripts/`，集成机运行副本在 `~/part2/scripts/`。

## 4. 交接包登记

已按《02》§2.4 规范导出到共享文件夹 `share/part2-handoff/`，供 #3 / #5 / #1 取用：

| 交接包 | 大小 | 流向 | 校验 |
|---|---|---|---|
| `ods` | 105 MB | → #3 | `manifest.json` + SHA-256 + dt 分区 + 注入日志，**全部通过** |
| `dws` | 3.9 MB | → #5 | 已产出 |
| `ads` | 5.5 MB（`ads.db` + `ads_manifest.json`） | → #2 / #1 | 已产出 |

**本次 run_id：`ads-20260914194754`**（生成机 `niyujun01`）。

## 5. 集成中发现并修复的问题

| # | 问题 | 处理 |
|---|---|---|
| 1 | 前端构建失败：`web/src/lib/*.js` 引用 `dashboard/assets/{contracts,models}.js`，导出时漏了这两个文件 | 从 `feat/part2-web` 补齐后构建成功 |
| 2 | 一键脚本里 `jps` 恒为 `0/5`：`bit` 用户 PATH 无 JDK，`sudo -u hadoop jps` 在非 login shell 下也找不到命令 | 改为 `sudo -u hadoop bash -lc "jps"`，实测恢复 `5/5` |
| 3 | **环境变量文件名与团队脚本不一致**：#4 的脚本 `source /etc/profile.d/ev-second-project.sh`，而本机装的是 `hadoop-env.sh` | 建软链接 `ev-second-project.sh → hadoop-env.sh` 兼容两套命名；已实测可加载 |

## 6. 阻塞项（需他人产出）

| 阻塞项 | 依赖方 | 影响 | 解除后可做 |
|---|---|---|---|
| **`handoff/dwd` 未交付** | **#3 PRL** | DWS/ADS 的 **Spark on YARN 正式链路**无法跑；当前 ADS 由 `build_local.py` 本地路径产出（后端自述为"过渡产物"） | 跑 `scripts/run_dws_ads.sh`，出 YARN 记录，替换过渡产物 |
| `handoff/forecast` 未交付 | #5 PE | 后端暂以 seasonal-naive 基线顶替（`forecastIsBaseline=1`） | 换真实 ML 批次，更新 `ads_meta` |
| 各分支未合并 | — | 集成机按目录取用，非统一基线 | 由 TL 决定合并到 `dev` 的时机 |

## 7. 结论与建议

1. **可演示状态已达成**：`bash scripts/start_part2.sh` 一条命令拉起全栈，Windows 浏览器可看大屏，符合《02》§6.5「一键拉起全栈」的形态要求；
2. **如实声明**：当前 ADS 为本地物化的过渡产物（`sourceKind=ods-handoff`），正式链路待 #3 的 DWD 到位后以 SparkSQL on YARN 重跑，届时**只换数据不改 Flask**；
3. **建议后续动作**：① 催 #3 交付 `handoff/dwd`；② 把各分支合并到 `dev` 并在集成机重跑一次；③ 把"环境变量文件名统一"写回《06》部署手册（当前靠软链接兼容）。

---

## 8. 2026-09-15 复验（对照老师新要求）

### 8.1 老师要求 × 现状对照

| # | 老师要求 | 状态 | 证据 |
|---|---|---|---|
| 1 | Python 3.11 / 3.12 | 用户明确不调整 | 系统 Python **3.10.12**（PySpark 3.5.7 官方支持 3.8–3.11） |
| 2 | **文件存储放到 hadoop 上** | ✅ **本轮补齐** | HDFS `/ev-charging` 已写入 ODS 104.5M / DWS 3.9M / ADS 5.5M，Spark 读回行数 = 8 |
| 3 | Spark 清洗分析 + **Flask web 请求处理** | ✅ | Flask 27 条契约路由（注册 28 条），契约自测 55 项全通过 |
| 4 | **Node.js 23+** / Vue3 | ✅ **本轮补齐** | Node **v23.11.1**（原 20.20.2）；Vue **3.5.13** |
| 5 | **使用 DataV 大屏展示** | ❌ **未实现**（待决策） | 现用 ECharts 5.6；评估报告见《09-DataV兼容性评估与改造方案》 |
| 6 | 机器学习预测 + 模型评估 | ✅ | #5 交付 `part2/ml/MODEL_REPORT.md` + `metrics.json` + `ads_forecast_batch` |

### 8.2 正式规模口径确认（#1 交办事项之一）

```
$ python3 part2/scml/scripts/check_scml_delivery.py --require-full
SCML delivery: ready
[OK] ods: ODS manifest/hash/partition/injection checks passed
[OK] dws: DWS four-table handoff is present
[OK] ads: ADS SQLite database is readable
[SKIP] dwd: handoff/dwd not delivered yet; D group reconciliation is skipped
[OK] scale: **formal delivery scale is confirmed**
[OK] reconcile: reconciliation passed
```

**结论**：正式规模口径已确认（`config/part2_scml_full.yaml`：8 站 / 288 桩 / 5000 用户 / 12 万订单 / 100 万遥测 / 窗口 `2026-06-17 → 2026-09-14`）；唯一未通过项是 **DWD 未交付**（#3）。

### 8.3 HDFS 存储验证（老师要求第 2 条）

| 目录 | 容量 | 校验 |
|---|---|---|
| `/ev-charging/ods` | 104.5 M | 含 `manifest.json`、`injection_log.json`、7 张表；`ods_orders` 等保持 `dt=YYYY-MM-DD` 分区（91 个分区目录） |
| `/ev-charging/dws` | 3.9 M | 四张汇总表 |
| `/ev-charging/ads` | 5.5 M | `ads.db` + `ads_manifest.json` |

**读回验证**：`spark-submit --master yarn` 读 `hdfs://niyujun01:8020/ev-charging/ods/ods_stations` → **行数 = 8**，YARN 作业 `FINISHED/SUCCEEDED`。

> 这一条同时解决了此前"数据只在本地磁盘、Hadoop 存储未用上"的缺口。

### 8.4 Node 升级与前端回归（老师要求第 4 条）

| 项 | 结果 |
|---|---|
| 升级方式 | 官方 tarball（清华镜像）→ `/usr/local/node`，软链到 `/usr/local/bin` |
| 版本 | **v23.11.1**（原 v20.20.2），npm 10.9.2 |
| 回归验证 | `npm ci` 65 包 / 11 秒 → `npm run build` **✓ built in 21.40s** |
| 结论 | 升级无回归，前端产物正常 |

### 8.5 一键脚本跨机适配（#1 交办事项之二）

统一入口 `scripts/part2/{30-start-demo.sh, 31-stop-demo.sh}`，并修正两处缺陷：

1. **`jps` 枚举不到守护进程**——集成机上 Hadoop 以 `hadoop` 用户运行，需 `sudo -u hadoop bash -lc "jps"`；
2. **误信启动脚本退出码**——以无权限用户运行 `start-dfs.sh` 会**返回 0 却不启动任何进程**，原逻辑误报成功；改为以"守护进程是否真的出现"为判据。

**实测**：全停 → 一键启动 → `守护进程: 5/5（jps 模式: sudo）`。

### 8.6 仍未完成的事项

| 项 | 阻塞方 | 说明 |
|---|---|---|
| `handoff/dwd` 交付 | **#3 PRL** | 全仓无 PRL 分支、无相关提交；DWS/ADS 的 Spark on YARN 正式链路与其对账（D 组）仍无法跑 |
| DataV 大屏改造 | 待用户决策 | 评估报告已出（《09》），推荐引入 `@kjgl77/datav-vue3` v1.7.4（703 KB） |
| `dev → main` 同步 | TL 决策 | #1 建议按上次 PR#9 的做法，可等答辩后再提 |
