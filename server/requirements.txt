# Flask 依赖（第二阶段后端）
#
# 刻意只依赖 Flask 本体 + 跨域中间件：
#   * 数据访问只用 Python 标准库 sqlite3，不引入 ORM / MySQL 驱动 / JDBC 桥；
#   * 不依赖 pandas / numpy，ADS 已在物化阶段算好（《02-TL》§3.2 结果物化取舍）。
#
# 安装：
#   python -m pip install -r server/requirements.txt
#
# 验证过的组合（2026-09-14，Python 3.13.12 / Windows）：
flask==3.1.3
flask-cors==6.0.5
