# 第 1 组 · 小组代码提交说明

> 压缩包：`BIT-SummerTermProject-submit-20260916.zip`（解压后为单一根目录 `BIT-SummerTermProject/`）
> 对应仓库：<https://github.com/ggggh123/BIT-SummerTermProject.git> 分支 `dev`（与本包内容一致）
> 生成方式：`git archive dev`（只含入库文件，天然排除缓存/日志/个人配置）+ 预构建前端 `web/dist`；按提交要求另剔除《概要设计说明书第一组.docx》《需求矩阵-第1组-王浩恩.xlsx》

## 1. 项目是什么

「城市能源指挥舱」——两阶段一体的新能源充电网络分析系统。

| 阶段 | 内容 | 主要目录 |
|---|---|---|
| 一阶段 | Qt/C++ 桌面端（用户端 + 管理服务端）、充电模拟器、C++ 测试 | `apps/` `simulator/` `shared/` `tests/` `CMakeLists.txt` |
| 一阶段 | SQLite 数据层、Python 数据流水线、ECharts 图表模型与页面 | `database/` `pipelines/` `dashboard/` |
| 二阶段 | 离线数仓（ODS→DWD→DWS→ADS）+ Spark MLlib 预测 + Flask 只读 API + Vue 可视化大屏 | `part2/` `server/` `web/` |

## 2. 目录导读

| 目录 | 内容 |
|---|---|
| `apps/` | 第一阶段 Qt 桌面端源码：`user-client/`（用户端）、`admin-server/`（服务端），C++/CMake |
| `dashboard/` | 第一阶段网页看板与图表模型（`assets/models.js` 为纯函数，被二阶段大屏复用） |
| `database/` `pipelines/` `simulator/` `shared/` `tests/` | 一阶段数据库脚本、数据流水线、模拟器、共享库与测试 |
| `part2/` | 第二阶段全部工程：`scml/`（数据生成/数仓/质量）、`ml/`（Spark MLlib 训练·预测·发布）、`quality/`、`scripts/`、`tests/`、`contracts/`、`plans/`（00–10 阶段设计文档） |
| `server/` | Flask 只读后端（27 条 `/api/*`，契约见 `docs/design/part2-api-contract.md`），同时托管前端 |
| `web/` | 第二阶段 Vue3 + ECharts + DataV 大屏源码；**含预构建产物 `web/dist/`**，免 Node 即可演示 |
| `docs/` | 设计、评审、测试证据与交付记录（362 个文件） |
| `handoff/` | **封存数据交付包**：ADS 只读库 + ML 预测包 + 清洗质量报告（详见 §6） |
| `runtime/golden/` | 第一阶段黄金基准库（受校验，桌面端与测试使用） |
| `references/` | 参考资料（Qt 教程、课程文档等） |

## 3. 快速跑起第二阶段大屏（推荐路径）

前置：**Python 3.10+**（实测 3.13.12）；**不需要 Node**（包内已带预构建 `web/dist`）；**不需要 Hadoop**（演示服务只读已物化的 ADS 库）。

```bash
# 1) 安装后端依赖（仅 Flask + 跨域中间件）
python -m pip install -r server/requirements.txt

# 2) 指向封存的 ADS 库并启动（默认 0.0.0.0:5000）
# Linux / macOS
ADS_DB="$(pwd)/handoff/ads/ads.db" python server/app.py
# Windows PowerShell
$env:ADS_DB="$PWD\handoff\ads\ads.db"; python server/app.py
```

浏览器打开：主页 <http://127.0.0.1:5000/> ｜ 用户视角 `/#/user` ｜ 充电站视角 `/#/station` ｜ 企业视角 `/#/enterprise` ｜ 社会视角 `/#/gov`

```bash
# 健康检查（会报告库路径、批次号、数据窗口、各表行数、路由数）
curl -s http://127.0.0.1:5000/api/health
```

Linux 演示机也可用一键脚本（带 PID 与日志管理）：

```bash
PART2_SKIP_HADOOP=1 bash part2/scripts/start_part2.sh   # 启动
bash part2/scripts/stop_part2.sh                        # 停止
```

可选：自行构建前端（Node ≥ 23）：`cd web && npm ci && npm run build:live`，产物覆盖 `web/dist`。

## 4. 第二阶段完整链路（可选，需要大数据环境）

依赖 Hadoop 3.4.1 + Spark 3.5.7 伪分布式（Ubuntu 22.04.5 已验证）：ODS 生成 → PRL 质量检测与清洗 → DWS/ADS 分层聚合 → Spark MLlib 训练与预测 → 发布进 `ads.db`。

- 环境搭建与跑批步骤：`part2/plans/06-TL-Hadoop伪分布式安装部署指南.md`、`docs/local-runtime.md`
- 中间层数据（`part2/scml/handoff/ods|dwd|dws`，ODS 约 107MB）**不随包提供**：体积大且可由仓库脚本重跑生成；封存结果已随包提供（见 §6）。

## 5. 第一阶段 Qt 桌面端（可选）

```bash
cmake --preset ubuntu22
cmake --build --preset ubuntu22
```

需要 Qt 工具链；基准库在 `runtime/golden/`；验证与测试记录见 `docs/test/`。

## 6. 随包封存数据（`handoff/`，约 5.7MB）

| 文件 | 说明 |
|---|---|
| `handoff/ads/ads.db` | **大屏实际读取的 ADS 只读库**（15 张表），sha256 记录在 `ads_manifest.json` |
| `handoff/ads/ads_manifest.json` | 封存批次 `ads-20260915235603`、数据窗口、各表行数、库哈希、质量与预测来源 |
| `handoff/forecast/*` | Spark MLlib 预测交付包：`run_id=f-20260915-232113`、`model_version=gbt-3.5.7`、`is_baseline=0`、144 行（6 站 × 24h），每个文件附 sha256（`manifest.json`） |
| `handoff/prl-20260915T133614Z-56524/` | PRL 清洗与质量报告：`quality_report.json/md`、`cleaning_report.json`、`contract.json`、`dwd-contract.json`、`policy.json`、`run_result.json`、`spark.log` |

答辩口径要点：

- 模拟数据窗口 **2026-06-17 → 2026-09-14（90 天）**；预测区间紧接其后（09-15 00:00–23:00，horizon 1–24）。
- 累计类指标（累计营收 / 累计充电量 / 累计订单 / 等效碳减排 / 等效植树）的累计起点由接口 `windowStart` / `windowEnd` 给出，大屏上已直接标注，避免被问「这些数从哪天算起」。
- 预测是真 ML 批次（`is_baseline=0`，gbt-3.5.7），不是 seasonal-naive 基线回退；每站恰好标记两个连续高峰小时。
- 全部如实标注的降级与假设（平均等待、首单新客、人口来源、服务半径、碳因子等）见 `server/README.md` §5.3 与 ADS 的 `ads_meta` 表。

## 7. 自检命令（可用于答辩现场复核）

```bash
python server/tools/selftest_contract.py         # 契约口径（信封/自洽/格式/错误码）
python server/tools/selftest_frontend_shape.py   # 前端字段结构对齐（mock 字段 ⊆ 接口）
python server/tools/selftest_static_dist.py      # Flask 静态托管与错误信封
npm --prefix web test                            # 前端纯函数回归
python -m pytest part2 part2/scml/tests part2/ml/tests -q   # 第二阶段单元测试
```

## 8. 本包未包含的内容及原因

| 未包含 | 原因 |
|---|---|
| `web/node_modules` | 第三方依赖，按已入库的 `package-lock.json` 用 `npm ci` 精确还原 |
| `runtime/dist`（38MB） | 第一阶段编译打包产物，由源码与 `CMakeLists.txt` 重新编译生成 |
| `part2/scml/handoff/{ods,dwd,dws}`（约 110MB） | 中间层大数据，可由 `part2/scml/**` 的生成器与 Spark 作业重跑生成 |
| `logs/`、`__pycache__/`、`.pytest_cache/`、`config.local.ini`、`.codebuddy/` | 运行日志、缓存与本机个人配置，与代码实现无关 |
| 《概要设计说明书第一组.docx》《需求矩阵-第1组-王浩恩.xlsx》 | 按本次提交要求未随包提供 |
